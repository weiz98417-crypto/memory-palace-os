# ADR-0012: 景区态势使用一秒基础 tick

## 状态

已接受

## 决策

模拟时钟以一秒为基础 tick，态势快照默认每秒刷新；设备、天气和客流信号按场景需要以一到五秒的间隔产生。倍速运行通过推进模拟时间实现，不通过无界面请求风暴实现。

## 原因

一秒粒度足以让地图和告警呈现实时变化，同时避免将每个动画帧写入业务数据库。信号按需产生可以区分高频态势读数与需要审计的业务事件。

## 影响

- 态势快照和业务事件必须分开存储与保留。
- 页面显示模拟时间和最近快照时间。
- 倍速、暂停、单步必须由模拟时钟控制器统一处理。
