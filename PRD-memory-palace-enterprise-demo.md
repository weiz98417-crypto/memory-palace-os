# Memory Palace OS 企业级演示版 PRD

> 文档状态：Release Candidate v1.0，候选实现与技术验收已完成，待产品负责人复核
>
> 适用范围：Memory Palace OS Enterprise Demo V1
>
> 运行基线：Windows + Docker Desktop；不要求宿主机安装 Python、Poetry 或 uv
>
> 重要约束：V1 只交付本机 Docker 企业演示链路；生产鉴权、真实外呼、多租户隔离和高可用仍属于后续生产化范围。

> 方案依据：候选实现以仓库 HEAD `a67de96` 为提交基线；Graphify 已于 2026-07-27 针对完整候选工作区刷新，包含 2388 个节点、3946 条边、188 个社区，未发现导入环。当前工作区快照见第 3.5 节，最终证据见 `docs/verification/enterprise-demo-v1/`。

---

## 1. Summary

Memory Palace OS 企业级演示版是一套可在普通开发电脑上通过 Docker 一键启动、无需真实企微或 LLM 密钥、能够稳定展示多智能体协作和企业治理能力的演示产品。

V1 的目标不是立即满足真实生产环境的全部要求，而是在 10 至 15 分钟内，用可重复、可解释、可追踪的业务剧本，向企业管理者、运营人员和技术评估者证明产品价值与架构可信度。

本版本的最高优先级依次为：演示稳定性、业务叙事清晰度、企业能力可见性、技术扩展可信度。任何不能直接提升这四项指标的功能均不进入 V1。

---

## 2. Contacts

| 联系人 | 角色 | 责任 | 备注 |
|---|---|---|---|
| ZhouWei（待确认） | 产品负责人 / 项目负责人 | 确认演示对象、业务剧本、验收范围和发布决定 | 以仓库作者信息为暂定依据 |
| Codex | 联合开发与技术执行 | 方案细化、代码实现、Docker 验证、测试和交付文档 | 在当前开发机协作 |
| 演示主讲人（待确认） | 演示使用者 | 评审讲解节奏、业务术语和现场操作复杂度 | 必须参加最终演练 |
| 技术评审人（待确认） | 架构与安全评审 | 确认演示声明与实际实现一致 | 可与项目负责人为同一人 |

### 2.1 决策方式

- 产品负责人决定 V1 范围和演示故事。
- 技术实现不得把模拟能力描述为真实生产能力。
- P0 范围变更必须先更新本文档，再进入开发。
- 文档中标记“待确认”的项目不阻止方案评审，但必须在对应开发阶段开始前确定。

---

## 3. Background

### 3.1 项目背景

Memory Palace OS 面向景区、娱乐场所及类似线下运营组织，解决现场消息分散、应急响应依赖个人经验、知识难以沉淀、任务执行缺少追踪的问题。

系统当前已经具备以下主体能力：

- FastAPI 网关与企微消息入口。
- Orchestrator 多智能体调度。
- Router、Commander、MemoryOps、Persona、PersonaExtract、Watcher、TodoWrite 等智能体。
- SQLite、PostgreSQL、Redis、ChromaDB 等基础设施代码。
- 权限、审批、任务图、上下文压缩、日志、健康检查和熔断器。
- 管理后台和覆盖多项能力的 Demo Console。
- 日常运营与突发事件种子数据。
- 154 个现有测试用例。
- Graphify 代码图谱，可用于理解依赖和维护架构；当前产物落后于仓库 HEAD，需要在开发基线阶段更新。

### 3.2 当前问题

项目已经拥有较多功能，但还没有形成一套可交付的企业演示产品。当前主要问题不是缺少功能，而是能力之间尚未完成稳定收口：

1. 新电脑无法仅依赖 Docker 按文档稳定启动 Demo。
2. 依赖清单、Docker 启动命令和环境变量存在漂移。
3. 一些企业化能力已经有代码，但没有完成集成和验收。
4. Demo 功能较多，但缺少统一场景控制、确定性结果和明确讲解主线。
5. 模拟调用与真实调用的界限不够清楚，容易造成错误的产品承诺。
6. 现有验证记录分散，无法在演示前快速判断系统是否可用。

### 3.3 为什么现在做

最近两轮开发已经补充了完整 Demo Console 和企业化基础模块。此时继续增加底层组件的收益低于把现有能力变成稳定演示闭环的收益。

现在适合进入“产品化收口”阶段，原因如下：

- 核心智能体和演示端点已经存在，可以复用。
- 现有 Graphify 快照显示核心模块关系已经形成，且在 `5f25cf6f` 未检测到导入环；该结论必须在当前 HEAD 重新生成图谱后复核。
- Docker Desktop 已经成为当前开发机可用的统一运行环境。
- 企业化 OpenSpec 尚未最终验收，适合按演示目标重新定义验收边界。

### 3.4 当前基线与差距

| 领域 | 已有基础 | V1 差距 |
|---|---|---|
| 启动 | Dockerfile、生产 Compose | 缺少可验证的 Demo Compose 和一键脚本 |
| 智能体 | 七类业务智能体及 Orchestrator | 缺少统一的场景执行契约和可重复结果 |
| Demo UI | 六标签控制台、Auto-play 基础 | 信息密度、错误状态、演示节奏仍需统一 |
| 数据 | SQLite、ChromaDB、两套种子数据 | 缺少幂等重置、数据版本和演示前检查 |
| 企业能力 | PG、Redis、认证、限流、日志、租户 | 部分能力未完整接线，不能全部声称已生产可用 |
| 验证 | 154 个测试用例 | 当前开发机尚未通过 Docker 执行完整回归 |
| 交付 | README、架构文档、Graphify 历史快照 | 图谱落后于 HEAD，且缺少统一的演示手册、验收报告和故障预案 |

### 3.5 当前工作区快照（2026-07-25）

当前分支为 `master`，提交基线为 `a67de96`。工作区已经存在一批未提交的候选实现，包括 Demo Compose、Scenario Controller、四个 YAML 场景、集成测试和新版 Demo Console。它们是在本 PRD 批准前产生的，因此统一按以下规则处理：

- 不把“文件已经存在”视为需求完成。
- 不在本轮文档工作中继续修改、删除、提交或回滚这些文件。
- 正式开发开始时，先逐项评审候选实现与本 PRD 的符合度，再决定保留、修改或替换。
- 所有通过结论必须在当前机器、Docker 容器和固定提交上重新产生证据。

| 工作区内容 | 当前观察 | 进入正式开发前的处理 |
|---|---|---|
| `src/memory_palace/demo/` | 已有未提交的 Controller、模型、目录、适配器和路由 | 对照 7.14 至 7.17 节做契约审查和测试复现 |
| `scripts/seed_data/demo/` | 已有四个未提交场景定义 | 由产品负责人确认业务术语、固定输入和预期结果 |
| `static/demo_console.html` | 已有大幅未提交改动 | 必须完成真实浏览器交互和三个目标视口的视觉验收 |
| `deploy/docker-compose.demo.yml`、`scripts/demo.ps1` | 已有未提交启动方案 | PowerShell 参数传递存在已知风险，需先修复并验证 Start/Status/Logs/Reset/Verify/Stop |
| `tests/integration/test_demo_scenarios.py` | 已有未提交场景测试；先前执行记录称 12 条通过 | 必须在固定镜像中重新运行并保存原始结果，不能只引用会话记录 |
| 全量测试 | 历史基线为 154 条；先前记录有 1 条路径校验失败 | 重新建立真实基线，区分历史失败、候选实现回归和新增失败 |
| Graphify | 报告仍基于 `5f25cf6f`，落后于提交基线和当前工作区 | 代码范围稳定后执行 `graphify update .`，复核节点、边、核心依赖和导入环 |

因此，当前项目状态应表述为“方案待批准、候选实现待审查、验收尚未完成”，不得表述为“企业级演示版已经完成”。

---

## 4. Objective

### 4.1 产品目标

交付一个“打开即可演示、过程可以解释、结果可以复现、架构经得起追问”的 Memory Palace OS 企业级演示版本。

演示版需要同时回答四个问题：

1. 它解决了什么企业运营问题？
2. 多个智能体如何分工并协作？
3. 每个判断和动作是否可以追踪、审计和复盘？
4. 从 Demo 升级到真实生产系统时，关键基础设施是否已有明确位置？

### 4.2 成功指标

#### Objective A：任何演示人员都能稳定启动

- KR A1：只安装 Docker Desktop 的电脑可以用一条命令启动 Demo。
- KR A2：镜像已经存在时，从启动命令到健康检查通过不超过 90 秒。
- KR A3：首次启动除下载 Docker 镜像外，不依赖其他宿主机工具。
- KR A4：默认 Demo 不需要真实企微、LLM、短信、PostgreSQL 或 Redis 密钥。
- KR A5：启动失败时，脚本给出明确的失败服务和处理建议。

#### Objective B：核心演示流程可重复

- KR B1：四个标准剧本均支持自动播放、单步执行和重置。
- KR B2：每个剧本连续执行 5 次，关键步骤和最终结果均成功。
- KR B3：重置操作在 10 秒内恢复到已知初始状态。
- KR B4：所有外部调用在 Demo 模式下使用确定性适配器，不因网络或模型随机性失败。

#### Objective C：企业能力可见且声明真实

- KR C1：每个业务步骤都展示 trace_id、执行智能体、状态、耗时和结果摘要。
- KR C2：界面可以查看 SLA、审计记录、知识引用、权限决定和系统健康状态。
- KR C3：模拟执行必须显示“模拟”标识，真实连接必须显示连接状态。
- KR C4：技术说明明确标记“已验证”“演示实现”“生产扩展位”三类能力。

#### Objective D：具备可交付质量

- KR D1：现有 154 个测试在 Docker 测试容器中全部通过；如基线数量变化，以测试发现结果为准并记录差异。
- KR D2：新增至少 4 条标准剧本端到端测试和 1 条冷启动冒烟测试。
- KR D3：在 1366x768、1440x900、1920x1080 三种桌面视口完成视觉验收。
- KR D4：形成演示手册、技术说明、验收报告和故障预案。

### 4.3 非目标

V1 明确不包含以下内容：

- 真实多节点高可用集群。
- 真实云短信、语音电话和企业微信生产账号交付。
- 完整企业 SSO、IAM 或细粒度 RBAC。
- 对外承诺的 7x24 生产 SLA。
- 大规模生产数据迁移。
- 经第三方认证的安全合规结论。
- 为了“看起来企业级”而增加无法演示或无法验证的组件。
- 将 Demo Mock 结果伪装成真实 LLM、短信或企微执行结果。

---

## 5. Market Segments

市场划分按用户要完成的工作，而不是按年龄、地区或公司规模划分。

### 5.1 企业决策者

**要完成的工作：** 在较短时间内判断产品是否值得继续投入、采购或试点。

**关注点：**

- 能否降低现场响应对个人经验的依赖。
- 是否可以沉淀组织知识。
- 是否有可见的效率和治理价值。
- 是否具备继续生产化的技术基础。

**约束：** 不会阅读代码，演示必须先讲业务结果，再讲架构。

### 5.2 现场运营管理者

**要完成的工作：** 快速处理突发事件，确保指令落实，并在事后复盘责任和经验。

**关注点：**

- P0/P1 事件是否能被快速识别。
- 指令是否明确、可执行。
- 超时是否会被发现。
- 历史 SOP 和老员工经验是否真正被使用。

**约束：** 操作步骤必须少，术语必须贴近现场业务。

### 5.3 企业技术评估者

**要完成的工作：** 判断系统是否有清楚边界、可维护性、安全意识和生产扩展路径。

**关注点：**

- 模块职责与依赖关系。
- 数据存储、消息队列、认证、审计和可观测性。
- Demo 与生产环境如何切换。
- 哪些能力已经验证，哪些仍是扩展位。

**约束：** 不能只看界面，需要能够查看执行证据和技术状态。

### 5.4 演示主讲人

**要完成的工作：** 在有限时间内顺畅讲完故事，现场不依赖开发人员救场。

**关注点：**

- 一键启动和一键重置。
- 自动播放和单步控制。
- 清晰的错误提示。
- 固定的输入、输出和讲解节奏。

**约束：** 现场网络可能不稳定，操作失误必须可恢复。

---

## 6. Value Propositions

### 6.1 对企业决策者

将分散在聊天记录、员工经验和纸面 SOP 中的运营知识，转化为能够参与实时决策、可以追踪和持续复用的企业资产。

用户获得：

- 更直观的 AI 投资价值展示。
- 从“单个聊天机器人”到“多智能体运营系统”的差异化认知。
- 可用于试点决策的业务闭环和技术证据。

用户避免：

- 只能展示对话、无法证明执行价值的普通 AI Demo。
- 依赖现场网络和外部模型而失败的演示。
- 用模糊的“企业级”宣传替代真实能力说明。

### 6.2 对运营管理者

把应急分诊、SOP 调用、任务分派、超时巡检和经验复盘连接成一条可见的执行链路。

用户获得：

- 更快的事件判断和标准指令。
- 重要步骤不再依赖口头转达。
- 每次事件都能形成新的组织记忆。

### 6.3 对技术评估者

以可运行代码证明系统具备清晰的调度核心、统一智能体契约、基础设施适配层、审计信息和降级策略。

用户获得：

- 可检查的 trace 和执行证据。
- 可从 Demo 后端切换到 PostgreSQL、Redis、ChromaDB 和真实 LLM 的路径。
- 清楚的已知限制，不需要猜测哪些能力只是演示。

### 6.4 价值曲线

| 评估维度 | 普通聊天机器人 Demo | Memory Palace OS 企业演示版 |
|---|---|---|
| 对话生成 | 高 | 高 |
| 多角色协作 | 低 | 高 |
| 业务流程闭环 | 低 | 高 |
| 知识引用与沉淀 | 中 | 高 |
| 审计与 SLA | 低 | 高 |
| 演示确定性 | 中 | 高 |
| 生产扩展说明 | 低 | 高 |
| 真实生产完备度 | 低 | V1 不承诺 |

---

## 7. Solution

### 7.1 总体方案

V1 采用“双运行层”设计：

1. **Demo Runtime：** 默认运行层。强调确定性、可重置、无外部密钥和一键启动。
2. **Enterprise Extension：** 技术展示层。保留 PostgreSQL、Redis、ChromaDB、认证、限流、租户和真实 LLM 的适配接口与状态说明，但未完成验证的能力不进入默认演示关键路径。

这两层共用相同的 Orchestrator、Agent 契约、业务模型和 trace 结构。差异只存在于基础设施适配器和功能开关。

```text
浏览器 Demo Console
        |
        v
Scenario Controller ---- Seed / Reset / Auto-play / Evidence
        |
        v
Gateway -> Orchestrator -> Router -> Business Agent -> Watcher
        |                       |
        |                       +-> Commander / MemoryOps / Persona / TodoWrite
        |
        +-> Trace / SLA / Audit / Approval
        |
        +-> Demo Adapters (默认、确定性)
        |      Mock LLM / InMemory Queue / SQLite / Local Knowledge / Mock Notify
        |
        +-> Enterprise Adapters (可选、状态可见)
               PostgreSQL / Redis / ChromaDB / Real LLM / WeCom / SMS
```

### 7.2 Docker 运行设计

Docker 是 V1 唯一受支持的开发和演示运行方式。

#### 7.2.1 默认 Demo 栈

目标命令：

```powershell
docker compose -f deploy/docker-compose.demo.yml up --build
```

默认栈应满足：

- 启动应用和演示所需的最少服务。
- 自动设置 `DEMO_MODE=true`。
- 自动初始化数据库和种子数据。
- 不读取真实 `.env` 密钥。
- 暴露一个固定本地地址，例如 `http://localhost:8000/demo`。
- 每个容器都有健康检查。
- 数据卷可以通过 reset 命令恢复到已知状态。

#### 7.2.2 企业技术验证栈

现有 `deploy/docker-compose.yml` 用于 PostgreSQL、Redis、ChromaDB 和 Nginx 的技术验证，不作为默认演示入口。

该栈必须明确显示：

- 哪些服务已经连接。
- 哪些服务正在使用降级实现。
- 哪些环境变量缺失。
- 哪些检查只验证连通性，未验证完整业务链路。

#### 7.2.3 辅助命令

V1 应提供 Windows PowerShell 脚本，并保留跨平台 shell 脚本：

| 命令意图 | 行为 |
|---|---|
| Start | 构建并启动 Demo，等待健康检查 |
| Stop | 停止服务，不删除数据 |
| Reset | 清理 Demo 数据并重新导入种子数据 |
| Verify | 执行健康检查、API 冒烟和关键场景测试 |
| Logs | 聚合显示应用和依赖服务日志 |

### 7.3 UX 与信息架构

#### 7.3.1 首屏：企业指挥中心

首屏直接展示可操作的系统，不制作营销落地页。

```text
+------------------------------------------------------------------+
| Memory Palace OS | 场景: 突发事件 | Demo Ready | 重置 | 自动演示 |
+------------------+-----------------------------------------------+
| 场景列表         | 当前事件: 一号门游客晕倒                     |
|                  | P0  处理中  SLA 00:12                         |
| - P0 应急        +-----------------------------------------------+
| - 知识问答       | Agent 执行时间线                              |
| - 经验萃取       | Gateway -> Router -> MemoryOps -> Commander   |
| - 任务拆解       |              -> Watcher                       |
|                  +-----------------------------------------------+
| 企业能力状态     | 当前步骤证据                                  |
| DB / Queue / LLM | 输入摘要 / 路由原因 / 知识引用 / 输出 / 耗时 |
+------------------+-----------------------------------------------+
```

#### 7.3.2 核心交互原则

- 主讲人始终可以看到当前场景、当前步骤和下一步动作。
- Auto-play、Pause、Stop、Reset 使用固定位置和熟悉图标。
- 时间线状态只使用 `等待 / 运行 / 成功 / 降级 / 失败` 五种语义。
- 失败时保留已经完成的步骤，并提供重试或重置。
- 每个 Agent 输出都能展开查看证据，但默认只显示短摘要。
- 模拟组件使用清楚的“Demo Adapter”状态，不使用容易误解的成功文案。
- 页面主要面向桌面演示，保证 1366x768 时无重叠和横向溢出。

### 7.4 标准演示剧本

#### Scenario 1：P0 应急事件处置

**目标：** 展示消息分诊、多 Agent 接力、SOP 调用、指令生成、SLA 和事后审计。

**固定输入：** “一号门有游客突然晕倒，现场围观人员很多。”

**标准步骤：**

1. Gateway 接收消息并生成 trace_id。
2. ContextTrigger/Router 判断为 `emergency_dispatch`，等级 P0。
3. MemoryOps 检索急救、现场隔离和人群疏导 SOP。
4. Commander 生成面向安保、医务和值班经理的三条指令。
5. Mock Notify 记录企微、短信和语音的模拟发送结果。
6. Watcher 检查响应是否满足 P0 SLA。
7. 事件、引用、指令和审计结果进入时间线和事件卷宗。

**确定性结果：** 路由、严重等级、三类指令、引用 SOP 和最终审计状态必须固定。

**企业证明点：** SLA、权限、审计、知识引用、降级通知。

#### Scenario 2：运营知识问答

**目标：** 展示组织记忆和带依据的知识复用。

**固定输入：** “连续降雨后，热门项目排队区应该先检查什么？”

**标准步骤：**

1. Router 判断为知识检索请求。
2. MemoryOps 从演示知识库返回三条最相关内容。
3. 系统展示来源、相关度、更新时间和适用场景。
4. Agent 基于引用内容生成行动建议。
5. 用户可以采纳建议并写入推送日志。

**确定性结果：** 至少包含地面防滑、临时围挡、客流监测三类检查项，并展示引用来源。

**企业证明点：** RAG、可解释回答、知识版本、采纳记录。

#### Scenario 3：老员工经验萃取与数字分身

**目标：** 展示隐性经验如何转为结构化资产并被再次调用。

**固定角色：** “张师傅，园区设备运营主管，15 年经验。”

**标准步骤：**

1. PersonaExtract 发起结构化访谈。
2. 使用预置回答完成四轮关键问题。
3. 系统提取 `trigger / behavior / reason` 逻辑条目。
4. 创建张师傅数字分身档案。
5. 向分身提出新的故障判断问题。
6. Persona 基于已经提取的逻辑条目，以第一人称回答并引用经验来源。

**确定性结果：** 至少生成三条逻辑条目，并完成一次有依据的分身回答。

**企业证明点：** 经验结构化、知识归属、数字分身、可追溯来源。

#### Scenario 4：复杂任务拆解与恢复

**目标：** 展示复杂目标拆解、任务依赖和失败恢复能力。

**固定输入：** “为周末夜游高峰制定并执行跨部门保障计划。”

**标准步骤：**

1. TodoWrite 把目标拆解为客流预测、人员排班、设备检查、应急预案和复盘任务。
2. TaskGraph 显示依赖关系和负责人 Agent。
3. 自动执行无依赖任务。
4. 在一个非关键任务中注入可恢复失败。
5. Watcher 或调度器发现失败并执行重试。
6. 全部任务完成后生成执行摘要和审计记录。

**确定性结果：** 任务数量、关键依赖、注入失败位置和恢复结果必须固定。

**企业证明点：** 任务图、进程内状态保留、失败恢复、跨 Agent 协作。

### 7.5 功能需求

#### P0：V1 必须交付

| 编号 | 功能 | 验收条件 |
|---|---|---|
| DEMO-BOOT-001 | Docker 一键启动 | 新环境只需 Docker 即可启动并通过健康检查 |
| DEMO-BOOT-002 | 启动前检查 | 检查端口、Docker daemon、Compose 和必要目录 |
| DEMO-DATA-001 | 场景数据初始化 | 首次启动自动导入版本化种子数据 |
| DEMO-DATA-002 | 幂等重置 | 任意状态下可恢复到相同的初始数据 |
| DEMO-SCENE-001 | 四个标准剧本 | 每个剧本支持 Play、Pause、Stop、Reset |
| DEMO-SCENE-002 | 确定性适配器 | 不访问外部服务也能得到固定业务结果 |
| DEMO-TRACE-001 | 统一执行记录 | 每步包含 trace_id、Agent、状态、耗时和摘要 |
| DEMO-TRACE-002 | 证据查看 | 可展开查看路由原因、知识引用和工具结果 |
| DEMO-UI-001 | 指挥中心 | 首屏展示场景、事件、时间线和企业能力状态 |
| DEMO-UI-002 | 错误恢复 | 失败后可重试当前步骤或重置整个场景 |
| DEMO-TRUST-001 | 模拟标识 | 所有 Mock 外部调用均明确标识 |
| DEMO-VERIFY-001 | 容器内回归 | 全量测试和场景测试通过 |

#### P1：强烈建议交付

| 编号 | 功能 | 验收条件 |
|---|---|---|
| DEMO-REPORT-001 | 演示报告导出 | 导出当前场景的步骤、结果、引用和指标 |
| DEMO-OPS-001 | 日志查看脚本 | 一条命令查看关键容器最近日志 |
| DEMO-OPS-002 | 环境状态页 | 显示 Docker 服务、适配器和数据版本 |
| DEMO-UI-003 | 单步讲解模式 | 主讲人可手动推进时间线 |
| DEMO-UI-004 | 全屏演示模式 | 隐藏非必要操作，适配会议投屏 |

#### P2：后续版本

- 真实企微沙箱接入。
- 真实 LLM 可切换演示。
- PostgreSQL、Redis 和 ChromaDB 的完整业务链验证。
- 多租户切换和数据隔离演示。
- Grafana 监控大屏联动。
- 演示录像自动生成。

### 7.6 场景执行契约

所有标准剧本应使用同一种场景描述格式。建议每个场景至少包含：

```yaml
id: emergency-p0
version: 1
title: P0 应急事件处置
initial_state: emergency
steps:
  - id: receive-message
    actor: gateway
    expected_status: success
  - id: route-message
    actor: router
    expected_output:
      intent: emergency_dispatch
      severity: P0
```

每一步必须定义：

- 稳定的 step_id。
- 执行主体。
- 输入来源。
- 预期状态。
- 预期输出中的关键字段。
- 最大等待时间。
- 是否允许重试。
- 前端展示摘要。
- 失败时的恢复动作。

### 7.7 数据设计

#### 7.7.1 数据分类

| 数据类型 | 示例 | V1 策略 |
|---|---|---|
| 场景定义 | P0 应急、知识问答 | Git 版本管理 |
| 种子业务数据 | SOP、事件、Persona | 启动或 Reset 时导入 |
| 执行数据 | trace、步骤、耗时 | 每次运行独立记录 |
| 演示配置 | 播放速度、故障注入 | 场景级配置 |
| 外部凭据 | 企微、LLM、短信 Key | V1 默认不需要，不进入仓库 |

#### 7.7.2 数据原则

- Reset 必须幂等。
- 场景数据必须带版本号。
- 同一个场景每次运行使用新的 trace_id。
- 业务输出可以确定，但不能伪造真实外部发送凭证。
- Demo 数据不得包含真实个人信息或企业密钥。

### 7.8 企业能力展示规则

界面和技术文档统一使用以下状态：

| 状态 | 含义 | 示例 |
|---|---|---|
| 已验证 | 已通过自动化或人工验收 | Docker Demo 启动、SQLite 数据重置 |
| 演示实现 | 使用确定性适配器完成业务展示 | Mock LLM、Mock 企微通知 |
| 可选连接 | 代码支持，但不在默认路径 | PostgreSQL、Redis、ChromaDB |
| 生产扩展位 | 已有设计或接口，尚未完成生产验收 | 多租户隔离、生产限流、真实短信 |
| 不可用 | 配置缺失或检查失败 | Docker 服务未启动、密钥缺失 |

禁止只使用绿色“正常”掩盖当前正在使用的 Demo Adapter。

### 7.9 可观测性与证据

每次场景运行至少记录：

- 场景 ID 和版本。
- trace_id。
- 开始、结束和总耗时。
- 每个步骤的输入摘要、输出摘要和状态。
- Agent 名称和路由原因。
- 使用的知识条目 ID。
- 权限检查结果。
- 外部动作类型及其真实/模拟状态。
- SLA 判断结果。
- 错误、重试和降级记录。

演示报告必须来自上述执行记录，不能只由前端拼接静态文案。

### 7.10 安全与信任

- 仓库和镜像不得包含真实密钥。
- Demo API 默认只绑定本机或明确的开发网络。
- 管理端点在 Demo 模式下可以简化认证，但页面必须显示当前安全模式。
- 所有输入在展示前进行 HTML 转义。
- 场景文件不能指定工作区之外的文件路径。
- Reset 只能清理明确的 Demo 数据卷和 Demo 数据表。
- 真实生产 Compose 不得使用演示默认密码作为交付配置。

### 7.11 兼容性

V1 必须支持：

- Windows 11 + Docker Desktop + PowerShell。
- Docker Compose v2。
- Chromium 系浏览器的最近两个主要版本。
- 1366x768 及以上桌面分辨率。

Linux/macOS 脚本为次要支持项，但 Docker Compose 文件应保持跨平台。

### 7.12 假设

以下内容目前是待验证假设：

| 假设 | 风险 | 验证方式 |
|---|---|---|
| 四个剧本可以在 15 分钟内讲完 | 内容过多导致重点分散 | 使用主讲人进行两轮计时演练 |
| 默认无需真实 LLM 也能证明价值 | 技术观众可能质疑结果真实性 | 明确 Demo Adapter，并提供可选真实连接说明 |
| 静态 HTML 足以完成 V1 | 单文件继续膨胀会降低维护性 | 先做 UI 评审，超出复杂度阈值再拆模块 |
| Docker 冷启动在目标现场可接受 | 镜像下载可能受网络影响 | 提供预拉取和离线镜像方案 |
| 当前 154 个测试可作为回归基线 | 测试可能与当前依赖不兼容 | 先在固定镜像中建立真实基线 |
| 当前种子数据足以支撑四个故事 | 数据内容可能不够真实 | 由业务负责人评审每个固定输入和输出 |
| Graphify 反映当前架构 | 现有图谱生成于 `5f25cf6f`，落后于 HEAD | Milestone 0 在当前 HEAD 执行 `graphify update .` 并复核节点、边和导入环 |

### 7.13 已知技术债务处理策略

V1 不要求一次解决全部技术债务，但以下问题会阻塞演示，必须优先处理：

1. Docker 启动命令与依赖声明不一致。
2. Demo Compose、Demo 环境配置和 Windows 启动脚本缺失。
3. README、`.env.example` 和代码中的环境变量名称不一致。
4. 限流、JWT 和压测相关依赖未完整声明。
5. Demo 数据重置和场景执行缺少统一契约。
6. 部分企业化能力在文档中标记完成，但尚无验证证据。

不阻塞默认演示、但必须标记为“生产扩展位”的问题：

- PostgreSQL 尚未覆盖所有直接访问 SQLite 的业务路径。
- 多租户过滤尚未完整接入查询层。
- 真实企微、短信和语音通知未完成供应商集成。
- 工作区隔离、上下文压缩仍包含后续阶段说明。

### 7.14 Scenario Controller 详细设计

#### 7.14.1 模块边界

新增一个独立的 `Scenario Controller`，作为演示场景的唯一编排入口。它负责场景目录、运行状态、步骤推进、暂停、停止、重置和证据汇总，不负责重新实现 Router、Orchestrator 或业务 Agent。

对外只暴露以下能力：

```python
class ScenarioController:
    def list_scenarios() -> list[ScenarioSummary]: ...
    def get_scenario(scenario_id: str) -> ScenarioDefinition: ...
    async def create_run(scenario_id: str) -> ScenarioRun: ...
    async def play(run_id: str) -> ScenarioRun: ...
    async def pause(run_id: str) -> ScenarioRun: ...
    async def step(run_id: str) -> ScenarioRun: ...
    async def stop(run_id: str) -> ScenarioRun: ...
    async def reset(run_id: str) -> ScenarioRun: ...
    def get_run(run_id: str) -> ScenarioRun: ...
```

内部可以调用现有 Orchestrator、TaskGraph、PersonaExtract、知识检索和 Watcher，但前端不得分别维护这些模块的运行状态。旧 `/demo/*` 端点在 V1 中保留兼容，不再作为新界面的状态源。

#### 7.14.2 运行状态机

```text
IDLE --play--> RUNNING --last step--> COMPLETED
  |                |  \
  |                |   +--error--> FAILED
  |                +--pause--> PAUSED --play/step--> RUNNING/PAUSED
  |                +--stop--> STOPPED
  +--step--> PAUSED/COMPLETED

PAUSED/COMPLETED/FAILED/STOPPED --reset--> IDLE
FAILED --step/play--> retry failed step
```

状态规则：

- 一个场景同一时间只允许一个活动运行，重复 Play 必须幂等。
- Pause 只在当前原子步骤完成后生效，不中断数据库写入。
- Stop 保留已经生成的证据，但不再执行后续步骤。
- Reset 只清理当前 Demo 场景的数据命名空间，重新加载同一版本种子数据。
- Step 每次最多推进一个步骤；步骤完成后回到 `PAUSED`，最后一步完成后进入 `COMPLETED`。
- 每个控制动作都返回最新版本号；前端用版本号避免旧轮询结果覆盖新状态。

#### 7.14.3 并发和生命周期

- 运行记录使用进程内 Store；V1 默认单 worker，避免多个进程状态不一致。
- Controller 内部按 `run_id` 加异步锁，防止双击和轮询造成重复执行。
- 活动运行最多保留 20 条，已结束运行最多保留 100 条；超出后按完成时间清理。
- 应用重启后活动运行不恢复，统一显示“运行已中断”，用户执行 Reset 后重新开始。
- 场景业务数据仍写入现有 SQLite；控制状态与演示证据使用独立 Demo 命名空间，避免污染真实业务数据。

### 7.15 API 契约

#### 7.15.1 端点

| 方法 | 路径 | 用途 | 主要返回 |
|---|---|---|---|
| GET | `/demo/scenarios` | 获取四个场景摘要 | 场景 ID、标题、时长、状态 |
| GET | `/demo/scenarios/{scenario_id}` | 获取场景定义 | 步骤、证明点、固定输入 |
| POST | `/demo/scenarios/{scenario_id}/runs` | 创建一次全新运行 | `run_id`、`trace_id`、`IDLE` |
| GET | `/demo/runs/{run_id}` | 获取运行快照 | 运行状态、当前步骤、证据列表 |
| POST | `/demo/runs/{run_id}/actions` | 执行控制动作 | 最新运行快照 |
| GET | `/demo/runs/{run_id}/report` | 导出演示证据 | JSON；浏览器打印版为 P1 |
| GET | `/demo/environment` | 获取环境状态 | 服务、适配器、数据版本、声明级别 |

`actions` 请求体：

```json
{
  "action": "play",
  "expected_version": 3
}
```

允许的 `action` 为 `play`、`pause`、`step`、`stop`、`reset`。版本不一致返回 HTTP 409 和最新快照；场景或运行不存在返回 HTTP 404；非法状态转换返回 HTTP 409；内部步骤失败保留 HTTP 200，并通过运行状态 `FAILED` 和结构化错误展示，因为运行失败不是 API 传输失败。

#### 7.15.2 轮询策略

V1 使用短轮询，不引入 WebSocket 或 SSE：

- `RUNNING` 时每 500 毫秒拉取一次运行快照。
- `PAUSED`、`FAILED`、`COMPLETED`、`STOPPED` 时停止自动轮询。
- 浏览器标签进入后台后将间隔降为 2 秒。
- 连续 3 次网络失败后显示连接中断，不改变后台运行状态，并提供重新连接。

选择短轮询是为了降低部署和恢复复杂度。实时流式推送不进入 V1。

### 7.16 统一数据与证据模型

#### 7.16.1 场景定义

```yaml
id: emergency-p0
version: 1
title: P0 应急事件处置
estimated_seconds: 150
fixed_input: 一号门有游客突然晕倒，现场围观人员很多。
business_outcome: 完成分诊、SOP 引用、三方指令和 SLA 审计
steps:
  - id: classify-event
    actor: Router_Agent
    adapter: deterministic-llm
    retryable: true
    timeout_ms: 3000
    presentation_delay_ms: 800
```

`presentation_delay_ms` 只控制演示节奏，不计入真实业务耗时。真实耗时必须由 Controller 在步骤执行前后测量。

#### 7.16.2 运行快照

```json
{
  "run_id": "run_01...",
  "trace_id": "trace_01...",
  "scenario_id": "emergency-p0",
  "scenario_version": 1,
  "status": "RUNNING",
  "version": 4,
  "current_step_id": "retrieve-sop",
  "started_at": "2026-07-25T13:30:00Z",
  "elapsed_ms": 1260,
  "progress": {"completed": 2, "total": 7},
  "steps": []
}
```

#### 7.16.3 步骤证据

每个步骤统一产生 `StepEvidence`：

```json
{
  "step_id": "retrieve-sop",
  "sequence": 3,
  "actor": "MemoryOps_Agent",
  "status": "SUCCESS",
  "started_at": "2026-07-25T13:30:01Z",
  "duration_ms": 84,
  "input_summary": "检索游客晕倒与现场疏导 SOP",
  "output_summary": "命中急救、隔离和疏导三条 SOP",
  "route_reason": null,
  "citations": [
    {"id": "SOP-EMG-001", "title": "游客突发疾病处置", "version": "1.2"}
  ],
  "tool_calls": [],
  "policy_decision": {"allowed": true, "rule": "demo-local-read"},
  "execution_mode": "DEMO_ADAPTER",
  "error": null
}
```

数据规则：

- 时间戳统一为 UTC ISO 8601；界面按本地时区显示。
- 枚举值在后端集中定义，前端不根据自由文本推断状态。
- 摘要用于首屏，完整输入输出只在证据抽屉展示。
- 任何模拟工具调用都必须记录 `execution_mode=DEMO_ADAPTER`。
- 报告直接序列化运行快照和证据，不在前端重建业务事实。

### 7.17 代码模块落位

根据当前代码和 Graphify 关系，V1 采用“新增深模块、薄路由、复用现有 Agent”的方式：

| 位置 | 计划职责 | 处理方式 |
|---|---|---|
| `src/memory_palace/demo/models.py` | 状态枚举、场景、运行和证据模型 | 新增 |
| `src/memory_palace/demo/catalog.py` | 加载并校验四个版本化 YAML 场景 | 新增 |
| `src/memory_palace/demo/adapters.py` | 确定性 LLM、知识、通知和故障注入 | 新增 |
| `src/memory_palace/demo/scenario_controller.py` | 状态机、步骤推进、重置、证据汇总 | 新增，唯一公共业务入口 |
| `src/memory_palace/demo/router.py` | 参数校验、HTTP 状态码、Controller 调用 | 新增薄路由 |
| `src/memory_palace/core/gateway.py` | 旧 Demo API | 仅兼容性接线，不继续堆场景状态 |
| `src/memory_palace/core/orchestrator.py` | 现有 Agent 调度 | 复用；只做必要注入点改动 |
| `src/memory_palace/core/task_graph.py` | 场景 4 任务依赖与重试 | 复用，不复制任务状态机 |
| `scripts/seed_data/demo/*.yaml` | 四个场景、SOP、Persona 和任务种子 | 新增版本化数据 |
| `static/demo_console.html` | 企业指挥中心 | 保留单页交付，重构布局和状态管理 |
| `deploy/docker-compose.demo.yml` | 默认确定性 Demo 栈 | 新增 |
| `deploy/Dockerfile` | 可配置基础镜像、运行与测试 target | 小范围修改 |
| `scripts/demo.ps1` | Start/Stop/Reset/Verify/Logs | 新增 Windows 入口 |
| `tests/unit/test_scenario_controller.py` | 状态机和幂等性 | 新增 |
| `tests/integration/test_demo_scenarios.py` | 四个剧本 API 端到端 | 新增 |
| `tests/integration/test_demo_reset.py` | 数据隔离和幂等重置 | 新增 |

不采用以下方案：

- 不把四个场景继续写成 `gateway.py` 中的分支。
- 不为 Demo 复制一套 Orchestrator 或 Agent 实现。
- 不在前端硬编码“成功时间线”绕过后端执行。
- 不为了演示默认启动 PostgreSQL、Redis、ChromaDB、Grafana 和 Nginx 全家桶。
- 不在 V1 引入前端构建链；现有单文件控制台足以交付，复杂度超阈值后再拆分。

### 7.18 企业指挥中心页面规格

#### 7.18.1 桌面布局

页面使用三栏工作台，不使用营销首屏和多层卡片：

| 区域 | 宽度/高度 | 内容 |
|---|---|---|
| 顶部状态栏 | 固定 56px | 产品名、Demo 标识、系统健康、数据版本、Reset |
| 左侧场景栏 | 240px | 四个场景、预计时长、当前状态、演示进度 |
| 中央主工作区 | 自适应，最小 620px | 事件摘要、运行控制、Agent 时间线、业务结果 |
| 右侧证据栏 | 340px | 当前步骤证据、引用、权限、模拟/真实状态 |
| 底部状态条 | 固定 32px | trace_id、连接状态、总耗时、版本号 |

在 1366x768 视口下，左栏可收窄到 208px，右栏可收窄到 300px；低于 1180px 时右栏改为可打开的抽屉。V1 不以手机作为演示主设备，但 768px 以上宽度不得出现内容重叠。

#### 7.18.2 视觉语义

- 背景使用低对比中性灰，内容区使用白色或近白色，不使用大面积深蓝或紫色渐变。
- 成功使用绿色，警告使用琥珀色，失败使用红色，运行使用蓝色；这些颜色只表达状态。
- 标题、正文和数据使用清楚的三级字阶；工作台内部不使用超大字号。
- 状态标签、按钮和时间线节点保持固定尺寸，状态变化不得推动布局跳动。
- 运行控制使用播放、暂停、停止、单步和重置图标，并提供中文 tooltip。
- 动画只用于当前步骤流转和进度变化；支持系统“减少动态效果”设置。

#### 7.18.3 完整状态

界面必须设计并验证以下状态：

- 首次加载、无活动运行、运行中、暂停、完成、业务失败、网络断开、重连中。
- 场景数据不可用、Demo 模式未开启、后端版本不兼容。
- Reset 二次确认、Reset 进行中、Reset 成功和 Reset 失败。
- 证据为空、引用为空、适配器降级和外部连接未配置。

### 7.19 测试与验证方案

#### 7.19.1 测试分层

| 层级 | 重点 | 最低覆盖 |
|---|---|---|
| 单元测试 | 状态转换、重复命令、步骤失败、证据序列化 | Controller 全部公开方法 |
| 契约测试 | API 状态码、JSON 枚举、版本冲突 | 7 个新端点 |
| 场景集成测试 | 四条固定输入到固定结果 | 每个场景至少 1 条完整路径 |
| 稳定性测试 | 重复运行、Reset、失败恢复 | 每个场景连续 5 次 |
| 容器测试 | 冷启动、健康检查、只依赖 Docker | 全新数据卷至少 1 次 |
| 浏览器验收 | 控件、状态、布局、无重叠、错误恢复 | 3 个目标桌面视口 |

#### 7.19.2 必测用例

1. Play 后步骤按定义顺序执行且只执行一次。
2. 连续两次 Play 不产生两条后台任务。
3. Pause 在当前步骤后生效，恢复后从下一步继续。
4. Step 只推进一个步骤。
5. Stop 后不再产生新证据，Reset 后回到干净 IDLE。
6. 注入的场景 4 失败按固定次数重试并成功恢复。
7. 任一场景执行失败后仍能查看之前的完整证据。
8. 版本冲突返回 409，前端自动刷新而不重复动作。
9. 不配置任何真实密钥时四个场景全部成功。
10. 所有通知和 LLM 结果明确标记为 Demo Adapter。
11. Reset 不删除非 Demo 会话、知识和任务。
12. 页面在 1366x768、1440x900、1920x1080 无重叠和横向滚动。

#### 7.19.3 容器验收命令目标

```powershell
# 启动
docker compose -f deploy/docker-compose.demo.yml up --build -d

# 验证
.\scripts\demo.ps1 verify

# 重置
.\scripts\demo.ps1 reset

# 停止
.\scripts\demo.ps1 stop
```

`verify` 必须返回非零退出码表示失败，并在控制台列出：Docker 服务状态、健康端点、四场景结果、测试通过/失败数量和报告位置。

---

## 8. Release

### 8.1 发布策略

采用分阶段交付，每个阶段都必须产生可运行结果。不得等到最后阶段才首次启动完整 Demo。

### 8.2 Milestone 0：建立真实基线

**预计：半天至 1 个开发日。**

交付内容：

- 冻结并盘点当前未提交候选实现，逐项给出保留、修改或替换结论。
- 固定 Docker Python 版本和依赖安装方式。
- 在容器内执行当前测试，记录真实通过、失败和跳过数量。
- 记录当前 Demo 启动失败点。
- 在候选代码范围稳定后更新 Graphify 图谱，记录生成提交、工作区状态、节点、边、核心枢纽和导入环结果。
- 建立“声明能力与验证状态”清单。

退出条件：

- 可以复现当前基线。
- 当前未提交候选实现均有明确处置结论，不会与后续工作混在同一提交中。
- Graphify 报告明确记录生成时的提交与工作区状态，并与待交付代码一致。
- 所有后续工作都能与基线比较。

### 8.3 Milestone 1：一键可运行

**预计：1 至 2 个开发日。**

交付内容：

- `deploy/docker-compose.demo.yml`。
- Demo 专用环境默认值。
- Windows PowerShell Start/Stop/Reset/Verify 脚本。
- 健康检查和启动等待。
- 依赖、启动命令和环境变量统一。

退出条件：

- 只安装 Docker 的新环境可以启动。
- `/health` 和 `/demo` 可访问。
- 不需要真实外部密钥。

### 8.4 Milestone 2：四个场景闭环

**预计：2 至 3 个开发日。**

交付内容：

- Scenario Controller 和统一场景契约。
- 四个版本化剧本。
- 幂等 Seed/Reset。
- 确定性 LLM、通知和知识检索适配器。
- 每步统一 trace 记录。

退出条件：

- 四个场景通过 API 端到端执行。
- 连续执行 5 次结果稳定。
- Reset 后不存在上一次运行残留。

### 8.5 Milestone 3：企业演示界面

**预计：2 至 3 个开发日。**

交付内容：

- 指挥中心首页。
- 场景控制与 Agent 时间线。
- 证据详情、知识引用、SLA 和审计视图。
- 企业能力状态区。
- 单步模式、全屏模式和报告导出。

退出条件：

- 三个目标桌面视口无重叠和横向溢出。
- 主讲人无需打开开发者工具即可完成演示。
- Mock 与真实状态不存在歧义。

### 8.6 Milestone 4：稳定性与交付

**预计：1 至 2 个开发日。**

交付内容：

- 全量测试修复。
- 四条场景端到端测试。
- Docker 冷启动冒烟测试。
- 浏览器交互与视觉验收。
- 演示讲解脚本、故障预案和验收报告。
- 更新 Graphify 图谱和项目 README。

退出条件：

- V1 成功指标全部有验证证据。
- 完成至少两轮完整人工演练。
- 从全新 Demo 数据状态可以在 15 分钟内完成讲解。

### 8.7 V1 范围

V1 包含：

- Docker 唯一运行基线。
- 四个确定性场景。
- 场景控制、时间线、证据和状态界面。
- Demo 数据初始化与重置。
- 模拟/真实状态标识。
- 自动化验证和交付文档。

V1 不包含第 4.3 节列出的非目标。

### 8.8 后续版本候选

#### V1.1：真实连接演示

- 可配置真实 LLM。
- 企业微信沙箱。
- PostgreSQL、Redis、ChromaDB 完整连接验证。

#### V1.2：企业试点

- 完整租户隔离。
- 生产认证与权限模型。
- 数据迁移和备份恢复。
- 部署监控与容量评估。

#### V2：生产化

- 高可用、灾备和多节点部署。
- 安全测试和合规评审。
- 真实供应商集成。
- 生产 SLA、容量模型和运维流程。

### 8.9 发布阻断条件

出现以下任一情况，不得标记 Enterprise Demo V1 完成：

- 新环境无法只用 Docker 启动。
- 任一标准场景无法重复执行。
- 页面把 Mock 调用展示为真实发送成功。
- Reset 会删除非 Demo 数据或作用范围不明确。
- 测试结果没有记录，或关键失败被忽略。
- 演示过程中必须手工修改数据库或容器内部文件。
- 当前能力状态与文档声明不一致。

### 8.10 最终交付物

1. 可构建的 Demo Docker 栈。
2. Windows 和跨平台辅助脚本。
3. 四套版本化场景与种子数据。
4. 企业指挥中心和演示控制台。
5. 场景执行记录和报告导出。
6. 自动化测试与验收报告。
7. 10 至 15 分钟演示讲解脚本。
8. 故障恢复和现场演示预案。
9. 更新后的 README、架构说明和 Graphify 图谱。

---

## Appendix A：建议演示节奏

| 时间 | 内容 | 目标 |
|---|---|---|
| 0:00-1:00 | 指挥中心和系统定位 | 让观众理解产品不是普通聊天机器人 |
| 1:00-5:00 | P0 应急场景 | 展示最强业务闭环 |
| 5:00-7:00 | 知识问答 | 展示组织记忆和引用依据 |
| 7:00-10:00 | Persona 经验萃取 | 展示隐性知识资产化 |
| 10:00-12:00 | 复杂任务拆解 | 展示执行和恢复能力 |
| 12:00-14:00 | 企业能力状态和架构 | 回答安全、扩展和运维问题 |
| 14:00-15:00 | 结果报告和下一步 | 形成试点或合作行动 |

## Appendix B：默认决策与评审项

除非评审时明确否决，V1 按以下默认值执行：

| 决策 | V1 默认值 | 原因 | 是否必须确认 |
|---|---|---|---|
| 产品负责人 | 仓库负责人 ZhouWei | 便于建立单一验收责任 | 发布前确认 |
| 默认地址 | `http://localhost:8000/demo` | 与现有服务端口一致 | 否 |
| 默认知识实现 | 本地确定性知识适配器，不启动 ChromaDB | 降低启动时间和外部镜像数量 | 否 |
| PostgreSQL/Redis | 在环境状态页显示为“生产扩展位”，不进入关键路径 | 当前业务调用尚未完整接线 | 否 |
| 报告导出 | P0 提供结构化 JSON，P1 提供浏览器打印/PDF | 先保证证据真实和可测试 | 否 |
| 界面语言 | V1 只交付简体中文 | 聚焦演示稳定性 | 否 |
| 外网依赖 | 运行期间零外网依赖；首次构建允许拉取镜像 | 满足常见现场网络不稳定情况 | 是，如需完全离线交付 |
| 前端技术 | 继续使用单文件 HTML/CSS/JS | 无需增加 Node 构建链 | 否 |
| 实时更新 | 500 毫秒短轮询 | 易部署、易恢复、足够支持演示 | 否 |
| 默认存储 | SQLite + 独立 Demo 数据命名空间 | 与现有可运行路径一致 | 否 |

开始开发前只需要产品负责人明确以下三项：

- [ ] 四个场景的业务术语、SOP 名称和固定结果符合目标客户语境。
- [ ] 演示现场是否要求在从未拉取镜像的新电脑上完全离线安装；如果要求，需要增加镜像导出包交付。
- [ ] 15 分钟完整版本是否为唯一脚本，还是还需要一套 5 分钟精简脚本。

## Appendix C：批准记录

| 版本 | 状态 | 评审人 | 结论 | 日期 |
|---|---|---|---|---|
| v0.1 | Draft | 待评审 | 初稿 | 待填写 |
| v0.2 | Draft | 待评审 | 补充状态机、API、数据模型、模块落位和验收追踪 | 2026-07-25 |
| v0.3 | Draft | 待评审 | 标明 Graphify 快照滞后，并将图谱更新加入开发基线门槛 | 2026-07-25 |
| v0.4 | Draft | 待评审 | 记录当前未提交候选实现，明确文档轮次冻结代码及正式验收前置条件 | 2026-07-25 |
| v1.0 | Release Candidate | 待产品复核 | 候选实现、Docker 验收、浏览器 QA、全量回归和 Graphify 刷新完成 | 2026-07-27 |

## Appendix D：需求与验收证据追踪

| 需求 | 自动化证据 | 人工证据 | 发布门槛 |
|---|---|---|---|
| DEMO-BOOT-001/002 | 冷启动脚本、`/health` 断言、退出码 | 新 Docker 环境启动记录 | 必须通过 |
| DEMO-DATA-001/002 | Seed 版本断言、连续 Reset、非 Demo 数据保护测试 | Reset 前后界面对比 | 必须通过 |
| DEMO-SCENE-001/002 | 四场景 API E2E、每场景连续 5 次 | 主讲人完整演练 | 必须通过 |
| DEMO-TRACE-001/002 | `StepEvidence` 契约测试、报告快照测试 | 随机抽查一条运行证据 | 必须通过 |
| DEMO-UI-001/002 | 浏览器交互用例、错误状态用例 | 三个视口截图和无重叠检查 | 必须通过 |
| DEMO-TRUST-001 | 适配器模式断言、报告字段断言 | 页面文案检查 | 必须通过 |
| DEMO-VERIFY-001 | Docker 内全量 pytest 报告 | 失败项审阅 | 零关键失败 |
| DEMO-REPORT-001 | JSON Schema/快照测试 | 打印版可读性检查 | P1，可延后 |
| DEMO-OPS-001/002 | 脚本退出码、环境 API 契约 | 故障演练 | P1 |
| DEMO-UI-003/004 | 单步/全屏浏览器用例 | 投屏演练 | P1 |

所有验收证据写入 `docs/verification/enterprise-demo-v1/`，至少包含：

- `test-results.md`：命令、镜像版本、通过/失败/跳过数量。
- `scenario-results.json`：四个场景连续运行结果。
- `startup-report.md`：构建、冷启动、健康检查和端口信息。
- `visual-qa.md`：视口、截图路径、发现和处理结论。
- `capability-matrix.md`：已验证、演示实现、可选连接、生产扩展位。

## Appendix E：实施顺序与变更闸门

| 顺序 | 工作包 | 前置条件 | 完成后必须证明 | 未通过时 |
|---|---|---|---|---|
| 0 | 候选实现审查与 Docker 基线 | PRD 获批 | 当前未提交改动有处置结论，测试和启动失败可复现 | 不进入功能开发 |
| 1 | 一键 Demo 栈 | 基线报告 | 只用 Docker 可访问 `/health` 和 `/demo` | 先修启动链 |
| 2 | Controller 最小切片 | Demo 栈健康 | 一个 P0 场景支持 Play/Step/Reset | 不并行铺开四场景 |
| 3 | 四场景与证据 | P0 切片通过 | 四场景 API E2E 和重复运行通过 | 固定后端契约后再做 UI |
| 4 | 指挥中心 UI | API 契约冻结 | 主讲人能完成控制、查看证据和恢复 | 不进入视觉润色 |
| 5 | 稳定与交付 | 功能冻结 | 全量回归、视觉 QA、文档和演练通过 | 阻止 V1 发布 |

每个工作包独立提交，提交说明包含对应需求编号。任何 P0 需求新增或范围扩大，先更新本 PRD，再开始实现。
> ⚠️ 历史规划文档：文中"ChromaDB 向量库 / 1536 维 embedding"等描述已被 ADR-0007 与 ADR-0009 取代。以此文档指导实现前请先读 ADR-0007 / ADR-0019。
