# Memory Palace OS 企业 MVP 交付 PRD

> 文档状态：Approved implementation baseline v1.1；内部验收进行中，尚未完成客户签收
>
> 产品阶段：Enterprise MVP，可交付真实试点客户使用
>
> 运行基线：Windows 11 / Linux + Docker Compose + Chromium 浏览器
>
> 产品入口：现有客户端 `static/index.html`
>
> 模型约束：所有生成式大模型调用统一使用 `deepseek-v4-flash`
>
> 密钥约束：API Key 只通过部署环境变量或 Docker Secret 注入，不进入代码、前端、镜像、日志或文档

---

## 1. Summary

Memory Palace OS 已经具备企业产品所需的功能厚度。本阶段不是制作 Demo，也不是继续堆功能，而是把现有客户端、多智能体、知识库、任务、审批、审计和企业基础设施收口为一个能够交付给真实试点客户持续使用的 MVP。

MVP 的完成标准不是“演示时看起来成功”，而是客户在没有开发人员陪同的情况下，可以部署系统、登录客户端、处理真实业务、查看真实状态、完成审批与闭环，并在服务重启后继续使用。所有需要生成式推理的路径必须真实调用 `deepseek-v4-flash`。任何尚未真实可用的功能必须被修复、禁用或隐藏，不能用动画、固定数据或假按钮代替。

本 PRD 中的 MVP 指“当前完整产品愿景的最小可交付版本”。由于项目已经拥有较多能力，MVP 的重点是可用性、连接性、可靠性和交付质量，而不是把功能数量缩减成一个单场景原型。

---

## 2. Contacts

| 联系人 | 角色 | 责任 | 备注 |
|---|---|---|---|
| ZhouWei | 产品负责人 / 项目负责人 | 确认 MVP 范围、客户价值、业务术语和发布决定 | 最终决策人 |
| Codex | 联合开发与技术执行 | 现状审计、实现、测试、Docker 交付和文档 | 当前开发机协作 |
| 试点客户负责人 | 业务验收 | 确认产品能完成真实工作 | 需要在试点前确定 |
| 客户系统管理员 | 部署与运维验收 | 验证安装、账号、备份、日志和恢复 | 需要参与交付演练 |
| 安全与技术评审人 | 技术验收 | 核对鉴权、数据隔离、密钥和能力真实性 | 不允许把占位实现声明为可用 |

### 2.1 决策原则

- 原客户端是正式 MVP 客户端，不另建平行演示产品。
- 现有功能厚度足够，除闭环阻塞项外不新增业务功能。
- 所有客户端可见功能必须真实可用，不允许假按钮和静态成功文案。
- 所有注册 Agent 必须进入真实业务路径，并有可追踪结果。
- 所有生成式推理统一使用 `deepseek-v4-flash`，不得静默切换其他模型。
- 企业基础能力必须用于真实运行，而不是只存在于代码和 OpenSpec 中。
- 外部渠道只有通过真实供应商或客户沙箱验收后才能标记“可用”。
- 未配置的外部能力必须显示“未配置”并禁用，不能返回伪造成功。
- MVP 可以是单机 Docker 部署，但必须支持安全重启、数据持久化、备份和恢复。

---

## 3. Background

### 3.1 产品定位

Memory Palace OS 面向景区、娱乐场所和类似线下运营组织，解决以下真实问题：

1. 现场消息分散，紧急事件容易被淹没。
2. 处置过程依赖少数老员工，组织无法复制经验。
3. 跨部门任务没有统一责任、时限和状态。
4. 通知、审批和操作缺少完整审计。
5. 事件完成后没有变成可复用的知识和 SOP。

产品的核心不是聊天，而是把“输入、判断、行动、监督、闭环、沉淀、复用”放进同一套企业工作系统。

### 3.2 现有能力基础

当前仓库已经包含：

- 现有 Web 客户端和管理后台。
- 企业微信 Webhook、消息队列和 Queue Worker。
- ContextTrigger、Router、Commander、MemoryOps、Persona、PersonaExtract、TodoWrite、Watcher 共 8 个 Agent。
- Orchestrator、会话状态、上下文分层、Agent 记忆和依赖注入容器。
- 知识检索、数字分身、任务图、工作区和工具执行器。
- FREE、LOGGED、APPROVAL 三级权限与审批流程。
- PostgreSQL、Redis Streams、ChromaDB 和 SQLite 兼容代码。
- API Key、JWT、租户隔离、限流、结构化日志和健康检查。
- LLM Wrapper、熔断、重试、输出消毒和 Fallback 框架。
- Docker Compose、测试、OpenSpec 和 Graphify 代码图谱。

这些能力说明项目已经超过普通功能原型阶段。当前缺口主要是产品整合和交付质量。

### 3.3 当前不能直接交付的原因

#### 原客户端没有贯通真实主链路

现有客户端主要读取管理 API。“添加记录”直接写入记忆表，不会经过队列、Orchestrator、Agent、权限或 Watcher。用户无法在正式客户端中完成一条完整业务链路。

#### 部分页面仍包含静态内容

巡检、SOP、图表和部分按钮存在硬编码或仅前端提示。页面外观已经存在，但业务行为不完整。

#### Demo 模式改变了产品行为

当前 `DEMO_MODE=true` 会隐藏原客户端和真实 API，并让 LLM Wrapper 返回假结果。该模式不适用于 MVP，应拆分为独立环境、渠道和模型配置。

#### 企业基础设施没有全部进入黄金路径

PostgreSQL、Redis、ChromaDB、认证、租户、限流等能力已有代码，但部分路径仍可能直接访问 SQLite、内存队列或模块级单例。代码存在不代表实际运行。

#### 交付运维不完整

当前缺少统一的数据库迁移、备份恢复、客户初始化、服务升级、日志诊断和故障恢复流程。

#### 外部集成仍有占位实现

企微回复、短信、语音和部分供应商调用需要真实凭据和沙箱验收。占位代码、Mock 或“已记录未发送”只能用于开发测试，不能作为 MVP 功能完成证明。

### 3.4 企业 MVP 的定义

本项目的企业 MVP 必须满足：

- 一个试点客户能够独立部署或由交付人员一次性部署。
- 真实用户可以使用正式账号登录。
- 用户可以在正式客户端完成主要工作，而不是使用 Swagger 或终端。
- 业务数据使用正式数据库持久化。
- 生成式 AI 使用真实 DeepSeek API。
- 服务重启后数据和未完成任务可恢复。
- 管理员可以查看健康、日志、队列和模型状态。
- 系统提供备份、恢复和安全升级方式。
- 可见功能全部可用；未配置功能清楚禁用。
- 有明确的试点边界，不把 MVP 声称为完整生产高可用版本。

### 3.5 MVP 与 Demo、生产版的区别

| 维度 | Demo | 企业 MVP | 完整生产版 |
|---|---|---|---|
| 用户 | 主讲人 | 真实试点用户 | 全量正式用户 |
| 数据 | 合成、可随时重置 | 真实试点数据、可备份恢复 | 正式数据治理和长期保留 |
| 模型 | 可 Mock | 真实 DeepSeek | 多供应商治理和容量保障 |
| 业务 | 预设脚本 | 用户自由输入并完成真实工作 | 全业务规则和复杂例外 |
| 外部集成 | 可 Stub | 已配置的渠道必须真实可用 | 多渠道生产 SLA |
| 稳定性 | 演示期间可用 | 支持持续试点使用 | 高可用、灾备、正式 SLA |
| 安全 | 本机隔离 | 基础认证、角色、租户和审计 | 完整 IAM、合规和安全运营 |
| 运维 | Start/Reset | 部署、升级、备份、恢复、诊断 | 自动化发布和完整运维体系 |

### 3.6 旧企业演示方案的处置

- `/demo` 独立页面不作为 MVP 产品入口。
- `ScenarioController`、Play/Pause/Step 和动画状态不进入 MVP。
- 固定场景 YAML 只能作为测试数据，不能代替真实用户输入。
- `_demo_results` 进程内结果缓存不能用于 MVP 状态。
- 单容器 SQLite Demo Compose 不作为交付栈。
- 错误 Demo 代码应在实施阶段通过可审计提交删除、归档或隔离，不使用破坏性 Git 重置。

---

## 4. Objective

### 4.1 产品目标

交付一个能在单个试点企业中投入真实使用的 Memory Palace OS MVP。客户能够通过现有客户端完成现场事件处理、知识复用、数字分身、任务协作、审批、巡检和经验沉淀，并由客户管理员完成日常运维。

### 4.2 试点规模假设

MVP 默认支持：

- 1 个试点企业。
- 1 至 3 个场地或 `venue_id`。
- 最多 50 个实名账号。
- 最多 20 个同时在线用户。
- 每日最多 5,000 条业务消息。
- 单机 Docker Compose 部署。
- 不承诺多节点高可用和跨地域灾备。

该规模是容量验收基线，不是商业授权限制。

### 4.3 核心结果

#### Objective A：试点用户可以独立完成工作

- KR A1：用户只使用正式客户端即可完成事件上报、查看、处置、审批和闭环。
- KR A2：用户可以自由输入业务内容，不依赖预设场景。
- KR A3：客户端刷新、浏览器关闭或 App 重启后，未完成工作仍可继续。
- KR A4：所有可见按钮都有真实后端行为、明确结果和错误处理。
- KR A5：客户完成基础培训后，不需要开发人员陪同使用。

#### Objective B：每个产品功能都真实可用

- KR B1：所有客户端导航、业务 API 和 8 个 Agent 进入权威功能注册表。
- KR B2：所有对用户可见的功能状态为 `READY` 或 `DISABLED_REQUIRES_CONFIG`，不存在假可用状态。
- KR B3：`READY` 功能都有用户入口、真实数据路径、权限、审计、错误状态和自动化测试。
- KR B4：所有 8 个 Agent 至少有一个正式业务入口，并可查看其输入、输出和 trace。
- KR B5：发布时用户可见功能可用率为 100%，不存在 `CODE_ONLY`、`STATIC_ONLY` 或假按钮。

#### Objective C：真实企业技术栈进入运行路径

- KR C1：PostgreSQL 是业务数据唯一权威存储；SQLite 不进入客户运行路径。
- KR C2：Redis Streams 是正式消息队列，支持 ACK、重试、去重和死信处理。
- KR C3：ChromaDB 保存正式知识向量并保留来源关系。
- KR C4：应用重启后事件、任务、审批、会话和 trace 可恢复。
- KR C5：所有依赖有健康检查，管理员可查看失败原因。

#### Objective D：所有生成式 AI 使用真实 DeepSeek

- KR D1：所有生成式调用的实际模型为 `deepseek-v4-flash`。
- KR D2：Router、Commander、MemoryOps、Persona、PersonaExtract、TodoWrite 和 Watcher 的模型路径均通过真实 API 验收。
- KR D3：记录模型名、耗时、token 和 provider request_id（如可用），不记录密钥。
- KR D4：模型不可用时明确失败、重试或转人工，不返回假 AI 结果。
- KR D5：不得回退到其他生成式模型；备用路由也必须使用 `deepseek-v4-flash`。

#### Objective E：具备企业基础安全

- KR E1：客户端登录由服务端验证，不在 JavaScript 中硬编码密码。
- KR E2：至少支持管理员、值班经理、普通操作员三种角色。
- KR E3：所有业务查询按 `venue_id` 隔离并通过跨租户负例测试。
- KR E4：关键操作需要权限检查并写入审计日志。
- KR E5：密钥不进入仓库、镜像、日志、页面、备份说明或验收截图。

#### Objective F：能够部署、运维和恢复

- KR F1：只安装 Docker 的目标服务器可以根据文档完成部署。
- KR F2：管理员可以执行 Start、Status、Migrate、Backup、Restore、Logs、Upgrade 和 Stop。
- KR F3：备份在独立环境成功恢复，并通过数据完整性检查。
- KR F4：应用完成至少 24 小时连续运行验收，无未处理崩溃和数据丢失。
- KR F5：核心 API 性能满足试点规模，错误率和 p95 有报告。

#### Objective G：试点交付成功

- KR G1：完成管理员手册、用户手册、安装手册、备份恢复手册和故障手册。
- KR G2：完成一轮内部 UAT 和一轮试点客户 UAT。
- KR G3：所有 P0 缺陷关闭，P1 缺陷有书面接受结论。
- KR G4：客户可以在验收环境独立完成端到端业务任务。
- KR G5：交付包包含固定版本、变更记录、配置模板和验收报告。

### 4.4 非目标

MVP 不包含：

- 多节点高可用和跨地域灾备。
- 正式 7x24 SLA。
- 完整企业 SSO、LDAP 和复杂组织架构同步。
- 第三方等保、渗透测试或行业合规认证结论。
- 大规模历史数据迁移。
- 移动原生 App。
- 任意客户自定义工作流设计器。
- 把尚未配置的供应商集成显示为已可用。
- 任何独立动画 Demo、Auto-play 或预制成功流程。

---

## 5. Market Segments

### 5.1 试点企业运营负责人

需要把现场事件从群聊和口头沟通转入统一系统，并看到责任、时限、审批和结果。

### 5.2 一线值班人员

需要快速上报事件、收到清楚任务、补充现场状态并完成闭环，不需要理解 Agent 或模型技术。

### 5.3 值班经理和审批人

需要看到事件依据、风险、责任人和高风险动作，并能批准、拒绝或转人工。

### 5.4 组织知识负责人

需要维护 SOP、萃取老员工经验、追踪知识来源并查看知识是否被实际采用。

### 5.5 客户系统管理员

需要管理账号、配置、健康、日志、备份和升级，并能判断故障发生在哪个组件。

### 5.6 企业技术评估者

需要确认客户端操作进入真实队列、模型、数据库和审计链路，基础安全和数据隔离真实有效。

---

## 6. Value Propositions

### 6.1 把现场消息变成可执行工作

系统自动识别、分级、检索知识、拆分任务和发起审批，让重要消息不再停留在聊天记录中。

### 6.2 把个人经验变成组织能力

老员工经验和已完成事件进入结构化知识体系，可以被新的事件和新的员工再次使用。

### 6.3 把 AI 判断变成可审计过程

每次路由、模型调用、知识引用、权限决定、任务变化和人工操作都有 trace 和审计记录。

### 6.4 把分散功能变成一个产品闭环

事件、任务、知识、Persona、审批、通知、Watcher 和 SOP 在同一个客户端中共享数据和状态。

### 6.5 用 MVP 验证真实业务价值

试点客户可以用真实数据和真实人员验证响应效率、闭环率、知识复用和经验沉淀，而不是评价一段预制演示。

---

## 7. Solution

### 7.1 产品原则

1. **客户任务优先。** 页面围绕上报、处置、审批、闭环和复盘设计。
2. **正式客户端唯一。** 所有用户工作在 `static/index.html` 所代表的产品中完成。
3. **真实数据驱动。** 页面只展示来自正式 API 和持久化数据的状态。
4. **AI 可追踪。** 所有生成式推理使用 DeepSeek，并保留模型证据。
5. **未配置即禁用。** 外部渠道没有凭据时显示未配置，不返回假成功。
6. **角色和租户先行。** 每个功能都定义谁可以操作和能看到什么。
7. **能恢复才可交付。** 数据、任务和配置必须支持备份、恢复和升级。
8. **停止功能堆叠。** 现有范围封板，只补齐真实可用性和交付能力。

### 7.2 正式客户端信息架构

| 产品区域 | MVP 职责 | 主要角色 |
|---|---|---|
| 登录与账户 | 服务端登录、会话、角色和退出 | 所有人 |
| 数据监控大屏 | 真实业务统计、SLA、待办和系统提醒 | 管理员、经理 |
| 现场事件 | 上报、查看状态、补充信息、处置和闭环 | 操作员、经理 |
| 全量记忆卷宗 | 检索已闭环事件、来源、结果和经验 | 经理、知识负责人 |
| 任务中心 | 查看任务图、负责人、依赖、失败和恢复 | 操作员、经理 |
| 审批中心 | 查看、批准、拒绝高风险动作 | 经理、管理员 |
| 推送与动作日志 | 查看企微、短信、语音等实际发送状态 | 经理、管理员 |
| 知识库 | 搜索、新增、编辑、删除、重建索引 | 知识负责人 |
| 数字分身 | 创建分身、访谈萃取、问答和删除 | 知识负责人 |
| 鹰眼巡检 | 管理策略、运行巡检、处理发现 | 经理、管理员 |
| SOP 中心 | 草稿、来源、版本、审批和发布 | 知识负责人、经理 |
| 系统管理 | 用户、角色、场地、渠道和基础配置 | 管理员 |
| 运维诊断 | 健康、队列、模型、版本、日志、备份 | 管理员 |

现有视觉风格和主要导航保留。新增区域必须融入同一客户端，不建设独立 MVP Console。

### 7.3 端到端正式业务流程

```text
用户客户端 / 企业微信
        |
        v
Canonical Ingress Service
        |
        v
Redis Streams -> Queue Worker -> Orchestrator
                                  |
        +-------------------------+--------------------------+
        |                         |                          |
 ContextTrigger / Router   Business Agents          Permission Engine
        |                         |                          |
        +-------------------------+--------------------------+
                                  |
                                  v
             PostgreSQL / ChromaDB / Audit / TaskGraph
                                  |
                                  v
                     正式客户端状态与操作
```

#### 7.3.1 事件上报

- 用户在客户端或企微提交自由文本。
- 系统生成 `message_id`、`trace_id`、`session_id` 和 `venue_id`。
- 客户端不能提交严重等级、路由 Agent 或最终状态。
- 消息进入 Redis Streams 后返回已受理状态。

#### 7.3.2 智能判断与处置

- ContextTrigger 判断是否需要触发。
- Router 识别业务意图。
- MemoryOps 检索相关 SOP 和历史经验。
- Commander 或其他 Agent 使用 DeepSeek 生成结构化结果。
- LLM 输出必须经过 schema 校验和安全消毒。

#### 7.3.3 任务与审批

- TodoWrite 把复杂目标或处置方案拆成 TaskGraph。
- 每个任务有负责人、依赖、状态、超时和审计。
- 高风险工具进入 APPROVAL 状态。
- 经理在正式客户端批准或拒绝。

#### 7.3.4 通知与执行

- 已配置渠道调用真实供应商或客户沙箱。
- 未配置渠道保持禁用，不生成假回执。
- 每次调用记录请求摘要、供应商响应、错误和 trace。

#### 7.3.5 闭环与知识沉淀

- 用户补充处置结果并关闭事件。
- 事件、任务和会话状态同步更新。
- 系统生成记忆内容和 SOP 草稿。
- 知识负责人审核后写入知识库和向量索引。

#### 7.3.6 Watcher 审计

- Scheduler 定时运行 Watcher。
- Watcher 查询未闭环、超时和违规记录。
- 审计结果进入客户端，并创建任务或审批动作。

### 7.4 功能范围

#### 7.4.1 业务功能

| ID | 功能 | MVP 必须支持 |
|---|---|---|
| MVP-BIZ-001 | 服务端登录与退出 | 正式账号、会话过期、错误提示和审计 |
| MVP-BIZ-002 | 监控大屏 | 真实统计、事件、SLA、Agent 和更新时间 |
| MVP-BIZ-003 | 现场事件上报 | 自由输入、附件说明、受理状态和 trace |
| MVP-BIZ-004 | 历史事件补录 | 与现场上报明确区分，记录来源 |
| MVP-BIZ-005 | 事件查询与详情 | 搜索、筛选、分页、关联任务和审计 |
| MVP-BIZ-006 | 事件处置与闭环 | 补充状态、责任人、结果和关闭 |
| MVP-BIZ-007 | 消息与会话 | 历史消息、会话状态和关闭 |
| MVP-BIZ-008 | 任务图 | 创建、依赖、分配、执行、失败和恢复 |
| MVP-BIZ-009 | 审批中心 | 待审批、批准、拒绝、意见和结果 |
| MVP-BIZ-010 | 推送与动作日志 | 渠道、收件人、状态、错误和采纳 |
| MVP-BIZ-011 | 知识搜索 | 向量检索、来源、版本和相关度 |
| MVP-BIZ-012 | 知识管理 | 新增、编辑、删除、导入和重建索引 |
| MVP-BIZ-013 | SOP 生命周期 | 草稿、审核、版本、发布和来源事件 |
| MVP-BIZ-014 | Persona 管理 | 创建、查看、搜索和删除 |
| MVP-BIZ-015 | PersonaExtract 访谈 | 多轮访谈、萃取、消毒和保存 |
| MVP-BIZ-016 | Persona 问答 | 使用已授权经验并显示来源 |
| MVP-BIZ-017 | Watcher 策略 | 查看、启停、修改和版本记录 |
| MVP-BIZ-018 | Watcher 发现 | 运行、查看、处理和关闭发现 |
| MVP-BIZ-019 | 用户、角色和场地 | 基础账号、角色和 `venue_id` 管理 |
| MVP-BIZ-020 | 系统设置 | 渠道、模型状态和非敏感配置 |

#### 7.4.2 Agent 功能

| Agent | 正式入口 | MVP 完成标准 |
|---|---|---|
| ContextTrigger | 所有消息入口 | 紧急、普通和排除消息均有正确行为 |
| Router | 所有已触发消息 | 能路由应急、知识、Persona 和任务请求 |
| Commander | P0/P1 事件 | 生成有知识依据的任务和指令 |
| MemoryOps | 知识问答和事件处置 | 返回来源明确的检索和建议 |
| Persona | 数字分身问答 | 只使用授权经验，回答可追溯 |
| PersonaExtract | 数字分身访谈 | 多轮状态和逻辑条目持久化 |
| TodoWrite | 任务中心和复杂事件 | 结构化任务和合法依赖写入 TaskGraph |
| Watcher | 定时和手动巡检 | SLA、超时和 SOP 审计可处理 |

#### 7.4.3 企业治理功能

- FREE、LOGGED、APPROVAL 三级工具权限。
- 审批批准、拒绝和审计。
- API Key 和 JWT 认证。
- 管理员、值班经理、操作员三类角色。
- `venue_id` 数据隔离。
- API 限流和 webhook 豁免。
- LLM 输出消毒。
- Agent 记忆隔离。
- 会话状态机和上下文预算。
- 工作区隔离和路径穿越防护。

#### 7.4.4 数据与可靠性功能

- PostgreSQL 正式后端和数据库迁移。
- Redis Streams 入队、消费、ACK、去重、重试和死信队列。
- ChromaDB 知识写入、查询、删除和重建。
- DeepSeek 实时调用、超时、重试和熔断。
- 技能注册、版本和安全热更新。
- 健康、就绪和 Prometheus 指标。
- JSON 结构化日志和 trace 关联。
- TTL Cache。
- Scheduler 定时任务。
- App 安全关闭和重启恢复。
- 备份、恢复和升级。
- 性能测试和容量报告。

#### 7.4.5 外部集成功能

- 企微 URL 验证和 AES 消息解密。
- 企微消息去重和回复闭环。
- 企微确认卡片。
- 短信通知。
- 语音电话。
- DeepSeek API。
- Embedding Provider。

外部集成只有完成真实供应商或客户沙箱验收后才能标记为 `READY`。如果交付时缺少供应商账号或凭据，该能力必须显示 `DISABLED_REQUIRES_CONFIG`，同时在交付清单中记录依赖和负责人。

### 7.5 全功能可用标准

#### 7.5.1 功能状态

每个功能必须进入机器可读注册表，并只能使用以下状态：

- `READY`：用户可以正常使用，自动化和 UAT 已通过。
- `DISABLED_REQUIRES_CONFIG`：代码完整，但缺少客户或供应商配置；客户端禁用并说明缺项。
- `BLOCKED`：存在产品或技术阻塞，不允许出现在交付版本可用功能中。
- `NOT_IN_MVP`：明确不在 MVP，客户端隐藏，文档不宣称可用。

禁止使用 `DEMO_ONLY`、`STATIC_ONLY`、`CODE_ONLY` 或“按钮能点但没有真实行为”的状态。

#### 7.5.2 单项完成定义

一个功能只有同时满足以下条件才能标记 `READY`：

1. 有明确用户和使用目的。
2. 有正式客户端入口或管理员入口。
3. 有服务端权限检查。
4. 有真实后端业务路径。
5. 使用正式数据库或基础设施。
6. 有加载、空数据、成功、失败、重试或禁用状态。
7. 有审计和 trace。
8. 有自动化测试。
9. 有用户验收步骤。
10. 有升级、备份或重置影响说明。

#### 7.5.3 可见功能可用率

```text
可见功能可用率 = READY 功能数 / 客户端可见功能总数 × 100%
```

MVP 发布要求可见功能可用率为 100%。缺少外部配置的功能可以显示在“集成设置”中，但业务按钮必须禁用，不能进入可见功能分母中的“可操作”状态。

#### 7.5.4 系统管理与诊断

MVP 需要正式的管理员诊断页面，而不是为了演示技术而新增的播放器。该页面支持：

- 查看 App、PostgreSQL、Redis、ChromaDB、DeepSeek 和渠道健康状态。
- 查看队列积压、消费组、失败消息和死信队列。
- 按 trace_id 查询结构化日志。
- 查看当前版本、数据库迁移版本和 Skill 版本。
- 查看模型名、最近调用状态、耗时和 token 指标。
- 执行安全的健康重检和失败消息重试。
- 发起备份并查看最近备份状态。
- 查看配置缺项，但不显示密钥内容。

认证负例、限流、租户隔离、路径穿越等安全能力通过自动化测试和交付验收证明，不在正式客户页面中提供“攻击按钮”。

### 7.6 DeepSeek 模型方案

#### 7.6.1 强制配置

```dotenv
OPENAI_API_KEY=<deployment-secret>
OPENAI_BASE_URL=https://api.deepseek.com/v1
LLM_DEFAULT_MODEL=deepseek-v4-flash
LLM_TIMEOUT=60
```

变量名称以现有 LLM Wrapper 为基线。后续可以重命名为中立的 `LLM_*`，但必须保持单一配置来源。

#### 7.6.2 调用范围

- Router 的语义路由。
- ContextTrigger 的二阶段判断。
- Commander 的处置指令生成。
- MemoryOps 的带依据建议。
- PersonaExtract 的提问、总结和逻辑萃取。
- Persona 的经验问答。
- TodoWrite 的任务拆解。
- Watcher 的语义审计和改进建议。

纯规则判断、权限、状态转换、SQL 查询和审计写入不调用模型。

#### 7.6.3 失败处理

- 启动时验证密钥格式、Base URL 和模型可访问性。
- 运行时统一超时、重试和 Circuit Breaker。
- 结构化结果必须通过 schema 和消毒。
- 连续失败后进入人工处理，不生成假结果。
- Fallback 路由如果启用，仍然只能调用 `deepseek-v4-flash`。
- Embedding 独立配置，不把生成式模型误称为向量模型。

### 7.7 数据架构

#### 7.7.1 权威数据源

- PostgreSQL：用户、角色、场地、消息、会话、事件、任务、审批、通知、审计、Persona、SOP 和 trace。
- Redis Streams：消息传输、消费确认、重试、去重和死信。
- ChromaDB：知识、经验和事件向量索引。
- 对象或工作区卷：导入文件、导出报告和任务工作区。

SQLite 只用于单元测试或明确的开发工具，不进入客户 MVP 运行路径。

#### 7.7.2 持久化要求

- trace 查询必须幂等，不能读取后删除。
- 任务、审批和会话重启后可恢复。
- 数据表使用正式迁移版本管理。
- 删除操作定义软删除或审计保留策略。
- 所有业务表包含 `venue_id`、创建时间和必要的操作者字段。
- 知识向量与关系数据之间保存稳定引用。

#### 7.7.3 备份与恢复

- PostgreSQL、ChromaDB 和配置元数据进入备份范围。
- Redis 不是唯一权威数据源，队列恢复策略必须明确。
- 提供手动备份命令和可配置定时备份。
- 备份文件加密或限制文件权限。
- 恢复必须在独立环境真实演练。
- 恢复后执行记录数、关键外键、知识索引和登录检查。

### 7.8 Docker 交付架构

| 服务 | 职责 | 网络 |
|---|---|---|
| App | FastAPI、正式客户端、Agent、Worker | 内部网络 + DeepSeek 出站网络 |
| PostgreSQL | 权威业务数据库 | 仅内部网络 |
| Redis | Streams 队列、去重和缓存 | 仅内部网络 |
| ChromaDB | 向量知识库 | 仅内部网络 |
| Nginx | HTTP 入口和安全响应头 | 对客户网络开放 |
| Backup Job | 数据库和向量库备份 | 仅内部网络和备份卷 |

要求：

- 数据服务默认不暴露宿主机端口。
- App 允许访问 DeepSeek，但不允许数据库直接出网。
- 默认不使用演示密码。
- 容器以非 root 用户运行。
- 所有服务有健康检查和固定镜像版本。
- Compose Project 名、网络和卷使用项目专用前缀。
- 升级前自动检查数据库迁移和备份状态。

### 7.9 认证、角色与租户

#### 7.9.1 MVP 角色

| 角色 | 权限 |
|---|---|
| 系统管理员 | 用户、角色、场地、配置、运维和全部审计 |
| 值班经理 | 事件、任务、审批、巡检、SOP 和业务报告 |
| 普通操作员 | 上报、查看授权事件、执行任务和补充结果 |
| 知识负责人 | 知识、Persona 和 SOP 管理，可由经理兼任 |

#### 7.9.2 认证要求

- 浏览器使用服务端会话或安全 JWT。
- API 客户端使用 API Key 或 JWT。
- 密码使用安全哈希保存。
- 登录失败有限流和审计。
- Token、Cookie 和 Key 不写入日志。
- 当前前端硬编码 `admin/123456` 必须移除。

#### 7.9.3 租户要求

- 所有业务请求建立 `venue_id` 上下文。
- 数据查询和写入都执行租户过滤。
- 管理员跨场地操作必须显式选择范围并记录审计。
- 自动化测试验证租户 A 无法读取或修改租户 B 数据。

### 7.10 外部集成策略

#### 7.10.1 客户端输入

正式客户端上报是 MVP 基础入口，不依赖企微凭据即可完成核心业务。

#### 7.10.2 企业微信

- 代码支持 URL 验证、AES 解密、消息去重和回复。
- 标记 `READY` 前必须使用客户测试应用或企微沙箱完成真实回调和回复验收。
- 缺少凭据时集成设置显示 `DISABLED_REQUIRES_CONFIG`。

#### 7.10.3 短信与语音

- 必须选择实际供应商并实现正式 Adapter。
- 标记 `READY` 前必须完成供应商沙箱或测试号码验收。
- 没有供应商配置时，短信和语音按钮禁用。
- 记录型 Adapter 只用于自动化测试，不能出现在客户业务结果中。

### 7.11 API 与状态模型

#### 7.11.1 核心 API 能力

- 登录、刷新会话和退出。
- 用户、角色和场地管理。
- 事件提交、查询、更新和闭环。
- trace 和阶段状态查询。
- 消息和会话查询。
- 任务图创建、分配、执行、重试和恢复。
- 审批查询、批准和拒绝。
- 通知与采纳状态。
- 知识 CRUD、导入、查询和重建索引。
- Persona CRUD、访谈和问答。
- Watcher 策略、运行和发现处理。
- SOP 草稿、审核、版本和发布。
- 健康、就绪、指标、版本、备份和诊断。

#### 7.11.2 通用状态

- `PENDING`
- `RUNNING`
- `WAITING_APPROVAL`
- `SUCCEEDED`
- `FAILED`
- `DEGRADED`
- `CLOSED`
- `CANCELLED`

状态由服务端维护。客户端不能提交最终成功状态，也不能用计时器自动推进。

#### 7.11.3 错误契约

每个 API 错误包含：

- 稳定错误码。
- 用户可理解的中文信息。
- trace_id。
- 是否允许重试。
- 建议操作。

敏感内部堆栈只进入受保护日志。

### 7.12 运维与可观测性

#### 7.12.1 管理命令

```powershell
scripts\mvp.cmd install
scripts\mvp.cmd start
scripts\mvp.cmd status
scripts\mvp.cmd migrate
scripts\mvp.cmd verify
scripts\mvp.cmd backup
scripts\mvp.cmd restore <backup-id>
scripts\mvp.cmd logs
scripts\mvp.cmd upgrade
scripts\mvp.cmd stop
```

#### 7.12.2 日志和指标

- 每个请求和后台任务都有 trace_id。
- 日志使用 JSON，可按组件、租户、用户和 trace 检索。
- 记录 API、Queue、Agent、LLM、数据库和外部渠道指标。
- 管理员页面显示队列积压、失败任务、模型错误和依赖健康。
- 告警至少覆盖服务不可用、队列持续积压、模型连续失败和备份失败。

#### 7.12.3 升级要求

- 固定应用和依赖版本。
- 升级前备份。
- 数据库迁移可重复执行并记录版本。
- 升级失败有回滚步骤。
- 客户配置与镜像分离。

### 7.13 安全要求

- API Key、JWT Secret、数据库密码和渠道凭据使用 Secret 注入。
- 仓库和镜像扫描不得发现真实密钥。
- 所有用户输入做长度、类型和 HTML 转义检查。
- LLM 输出写库或执行前做 schema 校验和消毒。
- 文件操作限制在任务工作区并阻止路径穿越。
- 管理 API 有认证、角色和限流。
- 关键业务操作写入不可轻易修改的审计记录。
- 客户端不显示敏感配置值。
- 备份和导出文件有访问控制。
- 依赖漏洞在交付前扫描并记录处置。

### 7.14 质量与验收

#### 7.14.1 自动化测试

- 所有现有测试通过，跳过项有明确批准。
- 每个产品功能至少一个成功测试和一个关键失败测试。
- 8 个 Agent 全部有结构化输出和异常测试。
- PostgreSQL、Redis、ChromaDB 使用真实容器做集成测试。
- 认证、角色、租户、限流和路径穿越有负例测试。
- 备份恢复和 App 重启恢复有自动化或可重复脚本。

#### 7.14.2 Live DeepSeek 验收

- 使用部署密钥真实调用 `deepseek-v4-flash`。
- 8 个 Agent 中需要模型的路径全部至少调用一次。
- 验证超时、无效结构和熔断恢复。
- 保存模型、耗时、token 和 request_id，不保存 Key。
- 离线 CI 可以使用 Mock Transport，但不能代替交付环境 Live 验收。

#### 7.14.3 浏览器 UAT

- 三种角色完成各自主要任务。
- 所有导航、表格、表单、弹窗和按钮真实工作。
- 空数据、加载、成功、失败、无权限和未配置状态均可理解。
- 刷新和重新登录后状态保持正确。
- 1366x768、1440x900 和 1920x1080 可用。
- 不需要打开 Swagger、开发者工具或终端完成业务操作。

#### 7.14.4 持续运行与性能

- Docker 环境连续运行至少 24 小时。
- 试点规模下 API 和页面 p95 有明确报告。
- 运行期间无数据丢失、任务重复执行或无法恢复的积压。
- DeepSeek 延迟单独统计，不与本地 API 性能混合。
- 使用 Locust 或等价工具完成容量验证。

#### 7.14.5 备份恢复验收

- 创建包含真实测试业务数据的备份。
- 在独立 Compose Project 中恢复。
- 用户可以登录。
- 事件、任务、审批、Persona、知识和 SOP 数量正确。
- Chroma 检索返回恢复前的知识。
- 恢复过程和耗时进入交付报告。

#### 7.14.6 客户可演示验收旅程

“可演示”是指用户在正式客户端使用自由输入和真实数据完成操作，系统实际经过正式 API、权限、队列、Agent、数据库和审计链路，并能展示成功、失败与恢复结果。不得使用 Swagger、数据库手工修改、预设动画、固定 JSON 或开发人员临时脚本代替产品行为。

发布前必须逐项完成以下旅程，并保存测试账号、输入、预期结果、实际结果、关键截图、trace_id、审计记录和自动化测试编号：

| UAT ID | 客户验收旅程 | 覆盖功能与能力 | 必须展示的结果 |
|---|---|---|---|
| MVP-UAT-001 | 登录、角色与工作台 | MVP-BIZ-001、002、019、020 | 三种角色登录；菜单和数据按权限变化；大屏读取真实统计；管理员完成用户、场地和非敏感配置管理 |
| MVP-UAT-002 | 现场事件智能受理 | MVP-BIZ-003、005、007；ContextTrigger、Router、Commander、MemoryOps | 用户自由输入事件；系统返回受理状态和 trace；完成触发、路由、知识检索和处置建议；页面可持续查看阶段和结果 |
| MVP-UAT-003 | 历史补录与记忆检索 | MVP-BIZ-004、005、011 | 历史补录不进入实时应急链路；来源被明确记录；随后能按条件和向量相关度检索，并查看来源与版本 |
| MVP-UAT-004 | 任务、审批与动作执行 | MVP-BIZ-008、009、010；TodoWrite、TaskGraph、Permission Engine | 方案拆成有依赖的任务；负责人执行；高风险动作等待审批；批准和拒绝均产生真实状态、动作日志和审计 |
| MVP-UAT-005 | 事件闭环、知识与 SOP | MVP-BIZ-006、011、012、013；MemoryOps | 事件补充结果并关闭；经验形成可审核知识或 SOP 草稿；完成编辑、审核、版本发布和再次检索复用 |
| MVP-UAT-006 | 数字分身完整生命周期 | MVP-BIZ-014、015、016；PersonaExtract、Persona | 创建 Persona；完成多轮访谈和逻辑萃取；刷新或重启后继续；使用已授权经验问答并显示来源；最后可搜索和删除 |
| MVP-UAT-007 | Watcher 巡检闭环 | MVP-BIZ-017、018；Watcher、Scheduler | 创建并启停策略；手动和定时运行；发现 SLA、超时或 SOP 问题；生成发现后完成分派、处理和关闭 |
| MVP-UAT-008 | 重启、失败与恢复 | Redis、PostgreSQL、ChromaDB、会话、任务、审批、死信 | 在处理中重启 App；状态不丢失且不重复执行；可重试失败任务和死信；知识检索仍有效 |
| MVP-UAT-009 | 多租户与安全边界 | 认证、角色、venue_id、限流、审计、工作区隔离 | 租户 A 无法读取或修改租户 B；越权和非法路径被拒绝；关键拒绝、删除和配置变更可审计 |
| MVP-UAT-010 | DeepSeek 与外部依赖降级 | deepseek-v4-flash、企微、短信、语音、健康诊断 | 展示真实模型调用证据；模型超时后进入重试、熔断或人工处理；已配置渠道真实发送；未配置渠道明确禁用且不产生假成功 |

覆盖规则：

- MVP-BIZ-001 至 MVP-BIZ-020 必须至少被一个旅程覆盖并标记 `READY`。
- 8 个 Agent 必须至少在一个旅程中产生可查询的输入、结构化输出、模型证据和 trace。
- 每个旅程必须由目标角色从正式客户端独立完成，业务步骤不依赖开发人员操作终端。
- 同一验收旅程至少包含一个成功路径和一个关键失败或无权限路径。
- 外部渠道只有真实沙箱或测试账号验收后才能声明 `READY`；缺少凭据时，只能验收“未配置且安全禁用”，不能宣称发送能力完成。
- 任何旅程未通过时，对应功能不能计入可见功能可用率，也不能用其他旅程的截图代替。

### 7.15 功能完整性门禁

发布前建立机器可读功能注册表。注册表至少覆盖：

- 20 项客户业务功能。
- 8 个注册 Agent。
- 权限、认证、租户、限流、审计和隔离能力。
- PostgreSQL、Redis、ChromaDB、DeepSeek 和外部渠道。
- 健康、日志、指标、调度、热更新、备份和恢复。
- 所有正式 API 和客户端可见动作。

功能注册表的每一项至少包含：

- 需求 ID、名称、产品负责人和当前状态。
- 正式客户端路由、可操作角色和租户范围。
- API、后台服务、涉及的 Agent 和权限等级。
- PostgreSQL 表、Redis Stream、Chroma collection 或文件卷等实际数据依赖。
- 审计事件名、trace 查询方式和错误码。
- 单元、集成、浏览器、Live DeepSeek 和 UAT 测试编号。
- 备份恢复影响、外部配置依赖、已知限制和阻塞原因。
- 代码提交、OpenSpec、用户文档和交付版本引用。

注册表不是静态清单。功能状态只能在上述证据齐全后变更为 `READY`，并由自动化校验阻止缺少入口、权限、测试或 UAT 证据的功能进入交付版本。

发布门槛：

- 用户可见功能可用率 100%。
- 8 个 Agent 全部 `READY`。
- 客户端不存在假按钮、固定成功结果或 Demo 动画。
- 所有 `DISABLED_REQUIRES_CONFIG` 项都有清楚配置清单，并且业务入口不可误触。
- 所有 P0 平台能力通过集成验收。
- README、PRD、OpenSpec、Graphify、客户端和 API 能力名称一致。
- 新增功能必须同时新增权限、审计、测试、文档和升级影响说明。

### 7.16 发布阻断条件

出现以下任一情况不得交付 MVP：

- 正式客户端无法完成端到端业务。
- 任一可见按钮没有真实后端行为。
- AI 输出来自硬编码、假模型或其他生成式模型。
- PostgreSQL、Redis 或 ChromaDB 被静默替换为本地 Demo 实现。
- 页面刷新或 App 重启后丢失业务状态。
- 登录仍由前端硬编码账号密码处理。
- 关键查询未执行租户隔离。
- 审批、通知、删除和配置变更缺少审计。
- 未配置渠道返回发送成功。
- API Key 出现在仓库、镜像、日志、页面或交付材料中。
- 数据库迁移、备份或恢复未验证。
- 必须依赖开发人员手工修改数据库才能使用。
- 存在未关闭 P0 缺陷。

### 7.17 假设与风险

| 假设或风险 | 影响 | 应对 |
|---|---|---|
| DeepSeek 网络波动 | AI 功能延迟或失败 | 超时、重试、熔断、人工接管和清楚状态 |
| 外部渠道缺少客户凭据 | 企微、短信或语音无法验收 | 提前列为交付依赖；缺失时禁用而非伪造 |
| PostgreSQL/Redis 接线不完整 | 运行中静默回落 | 对黄金路径做调用链审计并禁止客户环境 fallback |
| 当前客户端静态内容较多 | 用户误判状态 | 所有静态业务数据改为 API 或移除 |
| 功能范围较厚 | MVP 收口时间增加 | 功能冻结，只处理可用性、可靠性和交付阻塞 |
| 现有认证未接入客户端 | 企业安全不足 | 服务端登录和角色权限列为第一批工作 |
| 旧 Demo 代码继续干扰 | 路由和文档混淆 | 单独提交归档或删除并更新 Graphify |
| 当前 Key 已进入协作渠道 | 密钥风险 | 使用独立部署 Key，并在交付前轮换 |
| 单机 Docker 故障 | 试点服务中断 | 备份恢复、重启手册和清楚的 MVP SLA 边界 |

---

## 8. Release

### 8.1 发布策略

采用“功能冻结、恢复正式产品、打通主链、补齐全部功能、完成交付”的顺序。每个阶段都产生可运行版本，不等最后才做客户端联调。

各 Milestone 的开发日仅用于初始排期，不是完成承诺。Milestone 0 完成代码、数据和客户端审计后必须重新估算；后续阶段只按退出条件放行，不能因工期结束跳过真实链路、恢复、安全或 UAT 门禁。

### 8.2 Milestone 0：冻结范围并恢复正确基线

预计：1 个开发日。

交付：

- 冻结现有功能范围。
- 审计错误 Demo 提交与远端产品基线的差异。
- 恢复原客户端和正式 API 路由。
- 归档或删除 ScenarioController 和动画页面。
- 建立功能注册表、测试基线和 Graphify 基线。

退出条件：

- 正式客户端可访问。
- 当前每项功能有 `READY`、`PARTIAL`、`BLOCKED` 或 `NOT_IN_MVP` 初始状态。
- 未错误声明项目已经达到 MVP 完成状态。

### 8.3 Milestone 1：正式运行与安全基线

预计：2 至 3 个开发日。

交付：

- App、PostgreSQL、Redis、ChromaDB、Nginx 的 MVP Compose。
- 服务端登录、角色和租户上下文。
- DeepSeek 配置和 Live 预检。
- 数据库迁移、健康检查和结构化日志。
- 客户环境配置模板和 Secret 注入。

退出条件：

- 正式账号可以登录。
- 所有基础服务健康。
- App 真实调用 `deepseek-v4-flash`。
- 数据服务不暴露不必要端口。

### 8.4 Milestone 2：事件处理核心闭环

预计：3 至 5 个开发日。

交付：

- Canonical Ingress Service。
- 正式客户端事件上报和处置页面。
- Redis、Queue Worker、Orchestrator 和 Agent 主链。
- 持久化 trace、任务和事件状态。
- 审批中心和事件闭环。

退出条件：

- 用户可以自由输入事件并完成真实闭环。
- 刷新和 App 重启不丢失状态。
- Agent、知识、任务和审批可以追踪。

### 8.5 Milestone 3：知识、Persona、Watcher 和 SOP

预计：3 至 5 个开发日。

交付：

- 知识 CRUD、导入、检索和重建。
- Persona 创建、访谈萃取和问答。
- TodoWrite 和 TaskGraph 全流程。
- Watcher 策略、调度、发现和处理。
- SOP 草稿、审核、版本和发布。
- 推送与动作日志。

退出条件：

- 8 个 Agent 全部进入正式用户流程。
- 事件经验可以沉淀并再次复用。
- 所有页面使用真实 API 和数据。

### 8.6 Milestone 4：外部集成与运维交付

预计：3 至 5 个开发日，外部凭据等待时间不计入开发时间。

交付：

- 企微真实沙箱接入和回复验收。
- 短信、语音供应商 Adapter 和沙箱验收。
- 系统管理与运维诊断页面。
- 备份、恢复、迁移和升级脚本。
- 指标、告警、死信和失败恢复。

退出条件：

- 已配置渠道真实可用。
- 未配置渠道清楚禁用。
- 备份在独立环境恢复成功。
- 管理员可以独立完成日常诊断。

### 8.7 Milestone 5：全功能验收与客户 UAT

预计：3 至 5 个开发日。

交付：

- 全功能自动化和浏览器验收。
- 24 小时持续运行。
- 性能和容量报告。
- 三角色 UAT。
- 安全负例和租户隔离验收。
- 功能注册表封板。

退出条件：

- 用户可见功能可用率 100%。
- 8 个 Agent 全部 `READY`。
- 全部 P0 缺陷关闭。
- 客户可以不依赖开发人员完成验收任务。

### 8.8 Milestone 6：MVP 交付

预计：1 至 2 个开发日。

交付：

- 固定版本源码和 Docker 构建。
- 客户配置模板和 Secret 清单。
- 安装、用户、管理员、备份恢复和故障手册。
- 数据库迁移和升级说明。
- UAT、性能、恢复和安全验收报告。
- CHANGELOG、能力清单和已知限制。
- 与交付版本一致的 Graphify 图谱。

退出条件：

- 在目标环境完成一次从零部署。
- 使用客户或验收账号完成端到端业务。
- 双方确认 MVP 范围、限制和后续计划。

### 8.9 MVP 最终交付物

1. 正式 Memory Palace OS Web 客户端。
2. App、PostgreSQL、Redis、ChromaDB 和 Nginx 的 Docker 交付栈。
3. `deepseek-v4-flash` 真实模型接入。
4. 8 个 Agent 的正式业务入口。
5. 事件、任务、审批、知识、Persona、Watcher 和 SOP 完整闭环。
6. 用户、角色、租户、限流和审计。
7. 已配置外部渠道的真实集成。
8. 系统管理和运维诊断功能。
9. 数据库迁移、备份、恢复和升级工具。
10. 自动化测试、UAT、持续运行和性能报告。
11. 安装、用户、管理员、备份恢复和故障文档。
12. 功能注册表、已知限制、CHANGELOG 和 Graphify 图谱。

### 8.10 MVP 完成定义

只有同时满足以下条件，才能称为“企业 MVP 已交付”：

1. 真实试点用户可以使用正式账号登录。
2. 用户可以自由上报事件，而不是选择预设动画。
3. 事件进入 Redis、Orchestrator 和真实 Agent 链路。
4. 所有生成式步骤使用 `deepseek-v4-flash`。
5. 用户可以完成任务、审批、通知、处置和闭环。
6. 闭环事件进入记忆、知识和 SOP 流程。
7. Persona、TodoWrite 和 Watcher 可由正式客户端使用。
8. 所有用户可见功能真实可用或清楚禁用。
9. 服务重启后数据和未完成工作可恢复。
10. 客户管理员可以查看健康、日志、队列、模型和备份状态。
11. 备份可以在独立环境恢复。
12. 认证、角色、租户、限流和审计通过验收。
13. 系统完成持续运行和性能验收。
14. 客户可以在没有开发人员陪同的情况下完成 UAT。
15. 交付文档、版本、配置和已知限制完整。

达到这些条件，Memory Palace OS 才是可交付的企业 MVP，而不是 Demo、原型、功能集合或动画页面。

---

## Appendix A：实施前外部依赖

- 可访问 `deepseek-v4-flash` 的部署专用 API Key。
- DeepSeek Base URL 和网络出口。
- 企业微信测试应用或客户沙箱凭据。
- 短信供应商和测试号码。
- 语音供应商和测试号码。
- 试点企业的场地、角色和业务术语。
- 目标部署服务器、域名和 TLS 方案。

缺少外部依赖不阻止内部开发，但会阻止对应能力标记 `READY`。

## Appendix B：功能追踪原则

- 每个功能关联需求 ID、实现文件、权限、API、测试、文档和发布版本。
- 代码存在不代表功能完成。
- 页面存在不代表功能可用。
- 自动化通过不代表用户体验合格。
- Mock 成功不代表外部集成完成。
- 所有功能必须经过真实客户端或真实运维流程验收。

## Appendix C：当前文档优先级

本 PRD 是企业 MVP 的权威需求源。现有 `docs/enterprise-demo/`、`docs/verification/enterprise-demo-v1/` 和旧 Demo PRD 只记录已废弃方案及历史实现，不能覆盖本 PRD。
