# Core Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=22.0.0,<24.0.0
pydantic>=2.12.0,<3.0.0
pydantic-settings>=2.14.1,<3.0.0

# Async & Networking
httpx>=0.27.0
websockets>=12.0
aiofiles>=23.2.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
psycopg[binary]>=3.2.0
redis>=5.0.0

# Vector store: production embeddings come from TEI over HTTP (ADR-0019/0020).
# The local sentence-transformers rollback runtime is opt-in; see
# requirements-scenic-agent-local-embeddings.txt so the app image stays torch-free.

# Scheduling
apscheduler>=3.10.4

# Monitoring
prometheus-client>=0.20.0

# LLM / AI
openai>=1.13.0

# Scenic Agent trunk (ADR-0019 / ADR-0020).
# pydantic-ai-slim, never the pydantic-ai meta package: its openai extras require
# openai>=3.8, which conflicts with litellm's openai>=2.20,<3 cap.
pydantic-ai-slim==2.43.0
litellm==1.101.0
hatchet-sdk==1.40.1
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0

# Utilities
python-multipart>=0.0.9
loguru>=0.7.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
pytz>=2024.1
cryptography>=42.0.0
psutil>=5.9.0
watchdog>=4.0.0

# HTTP/2 support (httpx http2)
h2>=4.1.0

# Enterprise API middleware
slowapi>=0.1.9
PyJWT>=2.8.0

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
requests-mock>=1.12.0
